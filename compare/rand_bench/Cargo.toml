[package]
name = "alea-rand-compare"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
rand = { path = "../../../../Work/rand", features = ["std", "std_rng", "thread_rng"] }
rand_distr = "0.6.0"

[profile.release]
opt-level = 3
lto = "thin"
codegen-units = 1
debug = false
