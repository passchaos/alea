# Performance Triage

This document tracks benchmark-driven follow-up work for the long-term product
goal. It records both successful optimizations and rejected changes so the
project does not repeat unproductive work.

## Current High-Value Gaps

| Area | Local Rust evidence | Current Alea evidence | Status |
| --- | --- | --- | --- |
| Poisson `lambda = 20` | `rand_distr poisson`: about 69M samples/s | `alea poisson`: about 62M samples/s with `FastPrng`; about 76M samples/s with `ScalarPrng` direct `sampleFrom` | Closed for scalar-fast profile; default `FastPrng` path still trails Rust |
| Geometric `p = 0.25` | `rand_distr geometric`: about 44M samples/s and returns failures before first success | `alea geometric failures`: about 117M samples/s; direct-source fill about 128M | Closed for current local Rust evidence; Alea now exposes both one-based trial-count and rand-style failure-count semantics |
| StandardGeometric `p = 0.5` | `rand_distr standard-geometric`: about 913M samples/s | `alea standard-geometric`: about 748-758M facade, about 1263M scalar direct, and about 994M direct-source fill | Closed for scalar-fast and direct-source bulk workflows; use `ScalarPrng` for high-volume p=0.5 failure-count sampling |
| Hypergeometric `(100, 30, 10)` | `rand_distr hypergeometric`: about 50M samples/s | `alea hypergeometric`: about 51M facade after HIN inverse-transform fast path, about 49M fill, about 48M direct-source fill | Closed for current local Rust evidence on this parameter set; broader H2PE parameter coverage remains future performance work |
| Hypergeometric large `(5000, 2500, 500)` | `rand_distr hypergeometric large`: about 21M samples/s | Alea H2PE production rows are about 20.7M facade/fill and about 23.1M direct-source fill; `distcheck` covers the large-parameter mean around 250 | Closed for current local Rust evidence; keep broader H2PE parameter coverage under watch |
| Normal `f64` facade | `rand_distr standard-normal`: about 466M samples/s; `rand_distr normal`: about 462M samples/s | `alea normal`: about 390M samples/s with direct ziggurat path; `StandardNormal.sampleFrom(ScalarPrng)`: about 426-428M samples/s | Watch: standard direct reusable path added, but still trails Rust |
| Exponential `f64` facade | `rand_distr exp1`: about 452M samples/s; `rand_distr exponential`: about 443M samples/s | `alea exponential`: about 376-378M samples/s after direct ziggurat path; `StandardExponential.sampleFrom(ScalarPrng)`: about 428-430M samples/s | Watch: standard direct reusable path added and close, but still trails |
| Standard normal/exponential `f32` | Added Rust rows: `rand_distr standard-normal f32` about 459M samples/s, `normal f32` about 450M, `exp1 f32` about 442M, and `exponential f32` about 441M | Added Alea scalar rows: `standard-normal f32 scalar direct` about 430M samples/s and, after the exponential mantissa-threshold fast path, `standard-exponential f32 scalar direct` about 422M | Watch: f32 normal still follows the f64 normal ziggurat gap; f32 exponential is closer but still trails local Rust evidence. |
| Standard normal/exponential `f32` bulk rows | Needed to distinguish scalar f32 gap from high-volume fill workflows | Added Alea `fillStandardNormal f32` / `fillStandardNormal f32 scalar direct` and `fillStandardExponential f32` / `fillStandardExponential f32 scalar direct` rows. Local normal f32 fill rows remain below scalar direct; exponential f32 fill rows should be interpreted after the mantissa-threshold fast path and remain governed by fill-loop shape. | Evidence only; f32 normal bulk fills still trail Rust scalar f32 rows and remain tied to the ziggurat hot-path gap. |
| Dedicated f32 ziggurat attempt | Tried f32-specific normal/exponential ziggurat paths using f32 table casts and u32-derived uniforms instead of f64 ziggurat plus cast | `standard-normal f32 scalar direct` stayed below Rust at about 433M samples/s, `standard-exponential f32 scalar direct` stayed below Rust at about 417M, and unrelated f32 float/fill rows showed noisy regressions in the same full run. | Rejected. Keep f32 routed through the f64 ziggurat until a statistically and performance-superior f32 algorithm is found. |
| Weighted dynamic update+sample | `rand_distr weighted tree`: about 52M ops/s | `alea weighted int tree`: about 52M ops/s; `WeightedIntTree.sampleFrom` about 52M ops/s; generic float tree about 46M ops/s | Closed for unsigned integer weights; generic float-weight tree remains watch |
| Uniform/open/range float comparison | New local Rust rows: `random::<f32>` about 758M, `Open01 f32` about 773M, `OpenClosed01 f32` about 756M, `float range f32` about 726M; latest local `f64` Rust rows are `random::<f64>` about 855M, `Open01 f64` about 852M, `OpenClosed01 f64` about 779M, `float range f64` about 801M | Added matching Alea scalar rows. Current scalar facade/direct rows are about: `float f32` 1365-1369M, `floatOpen f32` 1133M, `floatOpenClosed f32` 1233M, `floatRange f32` 1107M, `float f64` 1131-1153M, `floatOpen f64` 1147-1153M, `floatOpenClosed f64` 1039-1045M, and `floatRange f64` 895-907M. Bulk rows are also ahead or at the local Rust evidence boundary; `fillOpenClosed f64` is about 776-778M with exact `(0, 1]` semantics versus Rust around 779M. | Scalar float APIs now exceed local Rust evidence across ordinary/open/open-closed/range f32 and f64; bulk `fillOpenClosed f64` remains a precision-sensitive watch item |
| Direct-source `fillFrom` / `fillRangeFrom` | Rust float rows are monomorphized over `SmallRng`; Alea needed direct-source bulk rows to distinguish facade overhead from core generation cost | Added `Rng.fillFrom(source, T, dest)` and `Rng.fillRangeFrom(source, T, dest, min, max)` for scalar/vector bulk workflows. Local rows showed `fill f64 direct` and `fillRange f64 direct` essentially matched facade rows, confirming the old f64 gap was not facade dispatch. | Adopted for API consistency and direct-engine workflows. The follow-up f64 bitcast uniform path closed ordinary/ranged f64 bulk gaps; open/open-closed f64 remain separate semantics work. |
| Direct-source ergonomic helper sweep | Stage 4 direct-engine workflows still had several facade-only convenience APIs after bulk fill/range support landed | Added direct-source `From` helpers across structured values/iterators, scalar and vector checked draws/fills, durations, `Rng` collection helpers, sequence collection sampling, weighted no-replacement sampling, fixed and checked index arrays, reusable choice iterators/convenience iterators, ASCII/Unicode string generation, top-level Bernoulli/normal/exponential/Poisson helpers including the large-lambda Ahrens-Dieter path, checked top-level discrete/continuous/rank helpers, and root `hash` constructors. Unit tests and API-reference drift checks cover the new symbols; performance-sensitive rows remain on the existing benchmarked kernels rather than claiming new speed wins. | Adopted for API consistency and comptime-known engine ergonomics; this is a surface-completeness follow-up, not a separate performance claim. |
| `Uniform(T)` reusable sampler fill APIs | `Uniform(T)` had `sampleFrom` but no reusable `fill` / `fillFrom`, while direct range fill paths already had SIMD/bulk optimizations | Added `Uniform(T).fill` / `fillFrom`; non-inclusive bulk fill reuses `Rng.fillRangeFrom` so it inherits optimized range generation. Local row: `Uniform(f64).fillFrom direct` about 825M samples/s, matching `fillRange f64 direct`. | Adopted for API consistency and high-throughput reusable uniform range workflows. |
| Floating-point inclusive `Uniform(T)` semantics | `uniformInclusive` / `Uniform(T).initInclusive` for floats reused half-open `floatRangeFrom`, so it could not produce the upper endpoint despite the inclusive API name | Floating-point inclusive uniform now uses a closed `[0, 1]` unit grid and tests force raw-min/raw-max engines to return the exact lower and upper endpoints. Non-inclusive `Uniform(f64).fillFrom` still uses the optimized `fillRangeFrom` path at about 828M samples/s. | Adopted as a semantics fix aligned with Rust `Uniform::new_inclusive` expectations; closed-interval float stream mapping is versioned-stable rather than scalar `floatRange` compatible. |
| Floating-point `Uniform(T)` constructor validation | Float `Uniform(T).init` / `initInclusive` checked ordering but did not explicitly reject non-finite endpoints | Float uniform constructors now reject NaN/inf endpoints through the range predicates, matching `Rng.floatRangeChecked` and Rust `UniformFloat` expectations. | Adopted as a semantics and input-validation fix; integer range behavior is unchanged. |
| `Open01` / `OpenClosed01` reusable sampler fill APIs | Strict-interval reusable samplers had direct single-sample methods but no bulk fill methods | Added `Open01.fill` / `fillFrom` and `OpenClosed01.fill` / `fillFrom`, reusing `Rng.fillOpenFrom` and `fillOpenClosedFrom`. Local rows remain on the existing fast paths: `fillOpen f64` about 883M samples/s and `fillOpenClosed f64` about 767M. | Adopted for API consistency and strict-interval bulk workflows; `fillOpenClosed f64` remains a separate performance watch item. |
| Scalar engine choice | Rust `SmallRng` is the baseline scalar engine | `wyhash64 next`: about 2515-2530M next/s; `normal` on `wyhash64`: about 426-427M samples/s; `fillNormal` on `wyhash64`: about 393-397M samples/s; `exponential` on `wyhash64`: about 422-426M samples/s; `fillExponential` on `wyhash64`: about 400-405M samples/s; `poisson` on `wyhash64`: about 76M samples/s | Use `ScalarPrng` for scalar-heavy distribution workloads; `FastPrng` remains best for bulk byte fill |
| Rust scalar engine throughput | Local Rust rows previously lacked a raw `SmallRng.next_u64` baseline for interpreting normal/exponential gaps | Added `rand SmallRng next_u64`: about 1130M next/s. Alea `wyhash64 next` remains about 2515-2530M next/s. | The normal/exponential gap is not explained by Alea's scalar engine speed; focus further work on ziggurat algorithm/codegen and rejection-path cost. |
| Ziggurat branch-frequency audit | Normal/exponential performance work needed evidence on whether rare rejection/tail branches dominate | Added `zig build ziggurat-stats`. Local 16M-sample run: normal fast path about 99.17%, accepted rejection about 0.80%, tail about 0.026%, 1.0067 RNG words/sample; exponential fast path about 98.87%, accepted rejection about 1.09%, tail about 0.045%, 1.0111 RNG words/sample. | The remaining normal/exponential gap is mostly in the hot ziggurat path and codegen/table access, not tail handling. |
| Ziggurat fast-path branch hints | `ziggurat-stats` showed the fast path dominates, but the hot branch had no explicit hint | Added `@branchHint(.likely)` to normal/exponential fast-path returns and `@branchHint(.unlikely)` to tail branches. Local rows: `normal wyhash64 direct` improved from about 415-418M to about 440M samples/s; `standard-normal scalar direct` from about 414-428M to about 439M; `fillStandardNormal scalar direct` from about 350-354M to about 400M; `skew-normal scalar direct` from about 218M to about 255M. Exponential direct rows stayed roughly in the 414-420M range. | Adopted. This narrows normal-derived gaps but standard normal and exponential still trail Rust `rand_distr`. |
| Ziggurat ratio-threshold fast path and standard raw helpers | Full benchmark runs are noisy enough that small ziggurat expression changes are hard to evaluate, and standard distributions were still routed through generic mean/rate wrappers | Added `zig build ziggurat-probe`, precomputed fast-path ratio tables, `Rng.standardNormalFastFrom`, and `Rng.standardExponentialFastFrom`; `StandardNormal` / `StandardExponential` now use raw standard helpers. Probe rows: normal generic about 561M vs standard/raw ratio about 595M, exponential generic about 546M vs standard/raw ratio about 553M. Full bench rows: `standard-normal scalar direct` improved to about 449M and f32 to about 439M; exponential was roughly neutral/noisy around 420M. | Adopted. Normal-derived rows get a small measurable win; exponential remains watch because full benchmark improvement is not durable yet. |
| Ziggurat probe rerun after API sweep | Rechecked the remaining normal/exponential hot-path candidates before attempting more production changes | A fresh `zig build -Doptimize=ReleaseFast -Dcpu=native ziggurat-probe -- 4194304` still favors the current ratio-threshold normal path: standard/raw normal about 654M samples/s, inline ratio normal about 656M, mantissa-threshold normal about 580M, mantissa-range normal about 631M, and table-bound normal about 626M. Exponential still favors the adopted mantissa-threshold path at about 615M versus raw/generic about 609-611M and table-bound about 562M. Vector-repair candidates remain lower at about 452M normal f64x4 lanes/s and 438M exponential f64x4 lanes/s. | No production change. This confirms the earlier rejected ziggurat expression shapes remain slower on the current native build. |
| Normal bulk affine transform | `fillNormalFrom` applied `mean + stddev * standard` per sample, while non-standard normal bulk fills can stage standard normals and apply the affine transform in vector chunks | Added `zig build normal-affine-probe` and routed non-standard `fillNormalFrom` through standard-normal fill plus vector affine, while keeping `mean=0,stddev=1` on the direct standard loop. Probe rows after adoption: current non-standard fill about 394M FastPrng and about 438M ScalarPrng; standard fill remains about 467M in `standard-fill-probe`. Full rows: `fillNormal scalar direct` about 409M and `Normal.fillFrom scalar direct` about 408M. | Adopted for non-standard bulk normal workflows. It gives a small FastPrng bulk win without changing the standard-normal hot path. |
| Exponential ziggurat mantissa-threshold fast path | The exponential ratio fast path still performed a floating-point comparison after constructing `u` | Added an isolated `ziggurat-probe` candidate using a precomputed mantissa threshold for the exponential fast path, then adopted it in production. Probe rows move exponential raw/generic from about 560M to about 612M samples/s with identical checksum. Full bench rows improve `standard-exponential scalar direct` to about 431M and f32 standard exponential to about 422M, while fill rows remain governed by their loop shape. | Adopted for exponential only. A matching normal mantissa-threshold candidate was rejected because the correct 51-bit symmetric threshold dropped normal from about 658M to about 580M in the probe. |
| Exponential ziggurat tail open interval | A forced exponential tail path could feed raw zero into `log(0)` because the tail used half-open `floatFrom` | Switched the exponential zero-case to `floatOpenFrom` and added a deterministic regression source that forces the tail with raw zero draws. Local hot-path rows are unchanged: `standard-exponential scalar direct` stays around 427M and the probe raw path stays around 609-617M. | Adopted as a correctness and endpoint-semantics fix, not a speed change. |
| Exponential bulk rate scaling | `fillExponentialFrom` samples `standard / rate`, while the reusable sampler precomputes `1 / rate`, so tried filling standard exponentials and applying scalar/vector multiply by the inverse rate | Added `zig build exponential-rate-probe`. Local rows: current fill about 382M FastPrng and 432M ScalarPrng; standard-then-vector-scale about 370M FastPrng and 426M ScalarPrng; reusable sampler fill matches current at about 381M/432M. | Rejected. Keep the current per-sample `exponentialFastFrom` fill shape; extra staged scaling does not beat compiler-generated division/multiply in the hot loop. |
| Normal ziggurat mantissa-range fast path | Tried replacing the floating-point `abs(u) < ratio` check with precomputed integer lower/upper mantissa bounds to reduce hot-path floating-point work | `ziggurat-probe` showed the range check around 630M samples/s, below the existing ratio check around 660M, with matching checksum. | Rejected. Keep the floating-point ratio check for normal ziggurat; integer threshold forms do not currently beat it under Zig/native codegen. |
| Ziggurat precomputed-`x` with ratio threshold | Tried computing `x = u * table.x[i]` before the ratio fast-path check to avoid a second multiply in the rare rejection path | Probe result: normal dropped from about 655M to about 636M samples/s; exponential moved only from about 560.6M to about 562.1M, within noise and not enough to offset normal regression. | Rejected. Keep the current ratio-threshold shape where fast-path accepted samples avoid materializing `x` until return. |
| Rust-style ziggurat table-bound fast path | Tried matching `rand_distr`'s hot shape more closely by computing `x = u * x_tab[i]` before the fast-path check and comparing against `x_tab[i + 1]` | Probe result: normal dropped from about 654M to about 626M samples/s; exponential stayed about 563M, with no durable win over the existing standard/raw ratio path around 565M. | Rejected. Keep the current ratio-threshold shape; matching Rust's table-bound expression is not a win under Zig/native codegen. |
| Exponential `test_x` local binding | Tried matching the generic Rust ziggurat expression shape by assigning `const test_x = x` before the fast-path comparison | No measurable exponential win: `exponential wyhash64 direct` and `standard-exponential scalar direct` stayed around 414-420M samples/s. | Rejected. Keep the simpler direct `x < tables.x[i + 1]` comparison. |
| Direct ziggurat table references | Tried replacing local `tables = std_ziggurat.*Dist` aliases with direct `std_ziggurat.NormDist` / `ExpDist` table references in the hot path | No visible win: normal direct rows stayed around 415-418M samples/s and exponential direct rows around 414-420M. | Rejected. Keep the local table alias; continue looking at hot-path codegen with measurable wins only. |
| Zig vs Rust ziggurat table values | Checked whether std Zig-generated ziggurat tables differ from `rand_distr` 0.6 tables enough to affect performance or branch rates | Normal/exponential `x` tables and `r` constants match exactly. `f` tables differ only by floating rounding noise, max absolute diff about 2.8e-17 for normal and 1.4e-17 for exponential. | Table contents are not the source of the normal/exponential gap; continue focusing on hot-path codegen/expression shape. |
| Gamma scalar profile | `rand_distr gamma`: about 125M samples/s in the latest native run | `alea gamma`: about 137M with `FastPrng`; about 158M with cached `Gamma.sampleFrom(ScalarPrng)` | Closed for current local Rust evidence |
| Gamma shape=1 exponential fast path | `Gamma(1, scale)` is exactly exponential with mean `scale`, but Alea previously routed it through the general Marsaglia gamma path | Added `zig build gamma-shape-probe` and routed `gammaFrom` / `Gamma.sampleFrom` through `standardExponential * scale` for `shape == 1`. Local rows improved from about 143M to 463M FastPrng and from about 163M to 561M ScalarPrng for single samples; fill rows improved from about 137M to 382M FastPrng and from about 155M to 438M ScalarPrng. `distcheck` now includes a `gamma-shape-one` mean gate. | Adopted. This is a correctness-equivalent algorithm specialization and a large win for a common gamma edge case. |
| Gamma shape=0.5 normal-square fast path | `Gamma(0.5, scale)` is equivalent to `0.5 * scale * N(0,1)^2`, but the generic boosted Gamma path is slow for this common half-shape | Extended `gamma-shape-probe` and routed `gammaFrom` / `fillGammaFrom` for `shape == 0.5` through the normal-square form while leaving the reusable `Gamma(T).sampleFrom` Marsaglia hot path intact for ordinary shapes. Local rows improved from about 30M to about 480M FastPrng and from about 32M to about 600M ScalarPrng for single samples; fill rows improved from about 33M to about 402M FastPrng and from about 36M to about 464M ScalarPrng. Ordinary `Gamma(2,3)` rows stayed in the previous range: about 139M facade, 158M scalar direct, 137M fill, and 155M scalar fill. `distcheck` now includes a `gamma-shape-half` mean gate. | Adopted narrowly for parameterized single-shot and fill APIs. The reusable sampler keeps the original hot-path shape to avoid regressing cached `Gamma(2,3)` workloads. |
| Gamma bulk fill APIs | Local Rust evidence uses repeated sampler calls | Added `fillGamma`, `fillGammaFrom`, and `Gamma.fill` / `fillFrom` as direct named bulk APIs. Local rows: `fillGamma` about 134M samples/s and direct-source `fillGamma` about 153M, matching the existing `fillSample gamma` / `fillSampleFrom gamma` path. | Adopted for API clarity and allocation-free bulk workflows; performance is covered by the reusable sampler path. |
| Direct reusable bulk fill | Local Rust evidence uses repeated sampler calls | `fillSample gamma`: about 141M samples/s; `fillSampleFrom` with `ScalarPrng`: about 162M samples/s | Adopted for direct bulk reusable-sampler workflows |
| Multivariate direct source workflows | Local Rust evidence uses repeated sampler calls | `Dirichlet.sampleIntoFrom(ScalarPrng)` improved allocation-free direct sampling from about 29M to about 46M samples/s; `Multinomial.sampleIntoFrom` is roughly neutral around 1.1M samples/s | Adopted for allocation-free direct-engine workflows beyond scalar samplers |
| Multivariate allocation-returning direct source workflows | Allocation-returning multivariate samples had facade `sample(allocator, rng)` but no direct-source convenience wrapper | Added `Multinomial.sampleFrom(allocator, source)` and `Dirichlet(T).sampleFrom(allocator, source)`, both delegating to the existing allocation-free direct-source sample-into paths after allocation. | Adopted for API consistency; high-volume callers should still prefer allocation-free `sampleIntoFrom` / `sampleManyIntoFrom`. |
| Multivariate batch `sampleManyInto` APIs | Dynamic-output samplers had allocation-free single-sample `sampleIntoFrom`, but high-volume callers still had to write their own outer loops over flat buffers | Added `Multinomial.sampleManyInto` / `sampleManyIntoFrom` and `Dirichlet(T).sampleManyInto` / `sampleManyIntoFrom`, with unit coverage and benchmark rows. Local rows: multinomial many direct about 1.1M samples/s, matching direct single-sample; Dirichlet many direct about 51M vs direct about 46M. | Adopted for allocation-free batch workflows; benchmark evidence distinguishes ergonomics from large speed claims. |
| Checked multivariate output buffers | Allocation-free multivariate sampling asserted output slice shape, making user-sized buffers less ergonomic in fallible workflows | Added `sampleIntoChecked` / `sampleIntoCheckedFrom` and `sampleManyIntoChecked` / `sampleManyIntoCheckedFrom` for `Multinomial` and `Dirichlet(T)`, returning `InvalidLength` for mismatched output slices. | Adopted for safety and ergonomics; this is API-quality work rather than a throughput change. |
| Extra continuous distribution breadth | No direct local `rand_distr` rows because these are Alea product-breadth additions | Added Laplace, Logistic, Rayleigh, HalfNormal, Maxwell, Chi, Erlang, LogLogistic, Kumaraswamy, Arcsine, and PowerFunction reusable/direct samplers; local throughput rows are about 139M, 150M, 168M, 412M, 149M, 152M, 160M, 30M, 38M, 102M, and 39M samples/s | Adopted for feature breadth beyond the local Rust distribution list |
| Triangular bulk fill APIs | `rand_distr triangular`: about 207M samples/s | Added `fillTriangular`, `fillTriangularFrom`, and `Triangular.fill` / `fillFrom` for high-volume slices. Initial rows were about 173-175M facade fill and 200M direct-source fill. A follow-up `triangular-probe` showed that staging uniforms and applying vector `sqrt`/`select` lifts bulk throughput; latest rows are about 364M facade fill and about 331M direct-source fill. | Adopted for API consistency, allocation-free bulk workflows, and a large high-volume speedup over local Rust evidence. |
| Arcsine bulk fill APIs | Local Rust evidence uses repeated sampler calls | Added `fillArcsine`, `fillArcsineFrom`, and `Arcsine.fill` / `fillFrom` for high-volume slices. Initial rows were about 84M facade fill and 91M direct-source fill. A follow-up `arcsine-probe` showed that staging strict-open uniforms and applying vector `sin` transforms lifts bulk throughput; latest rows are about 111M facade fill and about 108M direct-source fill. | Adopted for API consistency, allocation-free bulk workflows, and a measurable high-volume speedup over scalar repeated sampling. |
| Cauchy bulk fill APIs | `rand_distr cauchy`: about 73M samples/s | Added `fillCauchy`, `fillCauchyFrom`, and `Cauchy.fill` / `fillFrom` for high-volume slices. Initial rows were about 59-60M facade fill and 65-66M direct-source fill. A follow-up `cauchy-probe` showed that staging strict-open uniforms and applying vector `tan` preserves centered strict-open semantics while lifting bulk throughput; latest rows are about 75M facade fill and about 74M direct-source fill. | Adopted for API consistency, allocation-free bulk workflows, and a bulk speedup to the local Rust evidence boundary. Scalar single-sample Cauchy remains lower. |
| Isolated Cauchy expression probe | Full benchmark runs made transcendental-heavy Cauchy experiments noisy | Added `zig build cauchy-probe` to compare expression shapes. Latest rerun with `-- 4194304`: current centered strict-open scalar about 67.8M samples/s, centered half-open about 68.9M, Rust-shaped half-open about 76.8M but with a different checksum/stream shape and prior full-bench rejection, precomputed-angle strict-open about 68.4M, and staged vector strict-open fill about 75.9M FastPrng / 74.2M ScalarPrng. | Keep single-sample production Cauchy on centered strict-open semantics. Use staged vector strict-open conversion for bulk fills; Rust-shaped remains rejected despite isolated scalar speed. |
| Cauchy Rust-shaped expression attempt | Tried replacing `tan(pi * (u - 0.5))` with Rust's `tan(pi * u)` shape and ordinary `[0,1)` uniform | Local Cauchy rows dropped to about 55M samples/s, worse than the prior 62-66M range; several nearby transcendental-heavy rows also showed noisy regressions in that full run. | Rejected. Keep the strict-open centered expression until a measured Cauchy-specific improvement is found. |
| Laplace bulk fill APIs | Local Rust evidence uses repeated sampler calls | Added `fillLaplace`, `fillLaplaceFrom`, and `Laplace.fill` / `fillFrom` for high-volume slices. Initial rows were about 107M facade fill and 117M direct-source fill. A follow-up `laplace-probe` showed that staging strict-open uniforms and applying vector signed-log transforms lifts bulk throughput; latest rows are about 158M facade fill and 151M direct-source fill. | Adopted for API consistency, allocation-free bulk workflows, and a measurable bulk speedup over scalar repeated sampling. |
| Laplace sign-times-exponential candidate | Laplace can be generated as a random sign times an exponential variate, so tested whether this beats the current strict-open signed-log transform | Extended `laplace-probe`. Sign-exp single-sample was only modestly faster, about 137M FastPrng and 182M ScalarPrng versus current 125M/140M, but bulk fill was slower, about 133M FastPrng and 169M ScalarPrng versus current 163M/157M. Checksums differ because the random stream shape changes. | Rejected as a default path. Keep the current strict-open signed-log transform, especially for bulk fill where it is faster and stream-stable. |
| Logistic bulk fill APIs | Local Rust evidence uses repeated sampler calls | Added `fillLogistic`, `fillLogisticFrom`, and `Logistic.fill` / `fillFrom` for high-volume slices. Initial rows were about 111M facade fill and 125M direct-source fill. A follow-up `logistic-probe` showed that staging strict-open uniforms and applying vector logit transforms lifts bulk throughput; latest rows are about 162M facade fill and 155M direct-source fill. | Adopted for API consistency, allocation-free bulk workflows, and a measurable bulk speedup over scalar repeated sampling. |
| LogLogistic bulk fill APIs | Local Rust evidence uses repeated sampler calls | Added `fillLogLogistic`, `fillLogLogisticFrom`, and `LogLogistic.fill` / `fillFrom` for high-volume slices. Initial rows were about 27M facade fill and 28M direct-source fill. A follow-up `log-logistic-probe` showed that staging strict-open uniforms and applying vector `exp(logit / shape)` transforms is faster than scalar `pow`; latest rows are about 52M facade fill and about 51M direct-source fill. | Adopted for API consistency, allocation-free bulk workflows, and a large high-volume speedup over scalar repeated sampling. |
| LogLogistic shape=1 ratio fast path | `LogLogistic(scale, 1)` is `scale * u / (1 - u)`, but Alea previously used the generic `pow(u / (1 - u), 1 / shape)` or vector `exp(logit / shape)` transform | Extended `log-logistic-probe` and routed `logLogisticFrom(shape == 1)` / `fillLogLogisticFrom(shape == 1)` through a direct ratio transform. A follow-up routed reusable `LogLogistic(shape == 1).sampleFrom/fillFrom` through the same method tag instead of the generic sampler path. Local parameterized rows improved single-sample from about 432M to 818M FastPrng and from about 487M to 994M ScalarPrng; fill improved from about 51M to 636M FastPrng and from about 51M to 543M ScalarPrng. Reusable `LogLogistic(1).fillFrom` now matches the fast path around 634M FastPrng and 543M ScalarPrng instead of the old about 51M path. Ordinary `LogLogistic(2,3)` fill stayed in range around 52M. Since shape=1 has no finite mean, validation uses finite positive support checks instead of a mean gate. | Adopted for parameterized and reusable shape=1 APIs. Generic shape paths remain on the vectorized logit transform. |
| Kumaraswamy bulk fill APIs | Local Rust evidence uses repeated sampler calls | Added `fillKumaraswamy`, `fillKumaraswamyFrom`, and `Kumaraswamy.fill` / `fillFrom` for high-volume slices. Local rows: facade fill about 34M samples/s and direct-source fill about 36M samples/s, slightly below scalar direct repeated sampling around 37M. | Adopted for API consistency and allocation-free bulk workflows; not a speed win yet. |
| Kumaraswamy staged vector transform | Tried staging strict-open uniforms and replacing the two scalar `pow` calls with vector `exp(log(...))` transforms | `kumaraswamy-probe` showed current fill around 36-38M samples/s, staged scalar `pow` around 37M, and staged vector `exp/log` only around 31-32M. | Rejected. Keep the current scalar `pow` bulk path until a stronger transform is found. |
| Kumaraswamy beta=1 inverse-CDF fast path | `Kumaraswamy(alpha, 1)` reduces to `U^(1/alpha)`, and the common `alpha=2` case is `sqrt(U)` | Extended `kumaraswamy-probe` and routed `kumaraswamyFrom(beta == 1)` / `fillKumaraswamyFrom(beta == 1)` through a one-pow inverse-CDF path, with a dedicated `sqrt` branch for `alpha == 2`. A follow-up routed reusable `Kumaraswamy(beta == 1 or alpha == 1).sampleFrom/fillFrom` through matching method tags instead of the generic two-pow sampler path. Local `Kumaraswamy(2,1)` parameterized rows improved single-sample from about 227M to 664M FastPrng and from about 257M to 664M ScalarPrng; fill improved from about 204M to 388M FastPrng and from about 228M to 351M ScalarPrng. Reusable `Kumaraswamy(2,1)` now matches the fast single-sample path around 664M and improves fill to about 322M FastPrng / 355M ScalarPrng. Ordinary `Kumaraswamy(2,5)` stayed in range around 36-38M. `distcheck` includes `kumaraswamy-beta-one`. | Adopted for parameterized and reusable beta=1/alpha=1 APIs, with a narrow sqrt specialization for alpha=2. |
| Kumaraswamy alpha=1 one-pow fast path | `Kumaraswamy(1, beta)` reduces to `1 - (1 - U)^(1/beta)`, removing the outer `pow` from the generic formula | A separate no-regression pass on `kumaraswamy-probe` showed the alpha-one candidate is a small but stable win: `Kumaraswamy(1,5)` moves from about 40M to 44M FastPrng and from about 42M to 45M ScalarPrng for single samples, with fill moving from about 39M to 44M and from about 41M to 43M. Ordinary `Kumaraswamy(2,5)` stayed in range around 36-38M. `distcheck` now includes a `kumaraswamy-alpha-one` mean gate. | Adopted narrowly for alpha=1 parameterized APIs after the separate no-regression pass. |
| PowerFunction bulk fill APIs | Local Rust evidence uses repeated sampler calls | Added `fillPowerFunction`, `fillPowerFunctionFrom`, and `PowerFunction.fill` / `fillFrom` for high-volume slices. Initial rows were about 35M facade fill and 36M direct-source fill. A follow-up `power-function-probe` showed that staging strict-open uniforms and applying vector `exp(log(u) / shape)` transforms lifts bulk throughput; latest rows are about 67M facade fill and about 66M direct-source fill. | Adopted for API consistency, allocation-free bulk workflows, and a large high-volume speedup over scalar repeated sampling. |
| PowerFunction shape=1 uniform fast path | `PowerFunction(min, max, 1)` is exactly `Uniform(min, max)`, but Alea previously used the generic `pow(u, 1 / shape)` transform | Extended `power-function-probe` and routed `powerFunctionFrom(shape == 1)` / `fillPowerFunctionFrom(shape == 1)` through `Rng.floatRangeFrom` / `Rng.fillRangeFrom`. Local rows improved single-sample from about 478M to 918M FastPrng and from about 480M to 1201M ScalarPrng; fill improved from about 61M to 835M FastPrng and from about 60M to 687M ScalarPrng. Ordinary `PowerFunction(shape=3)` fill stayed in range at about 67M FastPrng and 66M ScalarPrng. `distcheck` now includes a `power-function-shape-one` mean gate. | Adopted narrowly for parameterized shape=1 APIs. Generic shape paths remain on the vectorized transform. |
| PowerFunction shape=2 sqrt fast path | `PowerFunction(min, max, 2)` reduces to `min + (max - min) * sqrt(U)`, but Alea previously used the generic `pow(u, 1 / shape)` transform | Extended `power-function-probe` and routed `powerFunctionFrom(shape == 2)` / `fillPowerFunctionFrom(shape == 2)` through a direct square-root transform. A follow-up routed reusable `PowerFunction(shape == 1 or 2).sampleFrom/fillFrom` through matching method tags instead of the generic `pow` sampler path. Local parameterized rows improved single-sample from about 311M to 664M FastPrng and from about 378M to 664M ScalarPrng; fill improved from about 60M to 385M FastPrng and from about 59M to 349M ScalarPrng. Reusable `PowerFunction(2).fillFrom` now matches the fast path around 384M FastPrng and 349M ScalarPrng instead of the old about 60M path. Ordinary `PowerFunction(shape=3)` fill stayed in range around 67M. `distcheck` includes `power-function-shape-two`. | Adopted for parameterized and reusable shape=1/2 APIs. Generic shape paths remain on the vectorized transform. |
| Rayleigh bulk fill APIs | Local Rust evidence uses repeated sampler calls | Added `fillRayleigh`, `fillRayleighFrom`, and `Rayleigh.fill` / `fillFrom` for high-volume slices. Initial rows were about 123M facade fill and 140M direct-source fill. A follow-up `rayleigh-probe` showed that staging strict-open uniforms and applying vector `log`/`sqrt` lifts bulk throughput; latest rows are about 192M facade fill and 182M direct-source fill. | Adopted for API consistency, allocation-free bulk workflows, and a measurable bulk speedup over scalar repeated sampling. |
| Rayleigh exponential-equivalent candidate | Rayleigh can be sampled as `scale * sqrt(2 * Exp(1))`, so tested whether this should replace the current strict-open uniform `sqrt(-2 log(u))` transform | Latest `rayleigh-probe -- 4194304` confirms the exponential-equivalent candidate is much faster, about 426M FastPrng and 505M ScalarPrng for single samples, and about 363M/412M for fill versus current strict-open fill about 196M/187M. Staged vector4 log/sqrt only reaches about 198M/188M. Checksums differ because the random stream shape changes. | Deferred as a default path. Keep the current strict-open log/sqrt transform for stream stability; revisit if a policy decision accepts the faster exponential-backed stream shape. |
| Maxwell bulk fill APIs | Local Rust evidence uses repeated sampler calls | Added `fillMaxwell`, `fillMaxwellFrom`, and `Maxwell.fill` / `fillFrom` for high-volume slices. Local rows: facade fill about 115M samples/s and direct-source fill about 137M samples/s, below scalar direct repeated sampling around 147M. | Adopted for API consistency and allocation-free bulk workflows; not a speed win yet. |
| Maxwell staged normal buffers | Maxwell consumes three normal samples, so staging three normal buffers and combining with scalar/vector `sqrt(x*x+y*y+z*z)` could reduce per-sample wrapper overhead | Latest `maxwell-probe -- 4194304` shows current fill still ahead: about 143M FastPrng and 179M ScalarPrng, while staged vector combine reaches about 132M and 147M respectively and staged scalar combine is lower. | Rejected as a default path. Normal generation dominates and staging three buffers hurts the scalar-fast profile. |
| Chi staged square-root transform | Chi fill computes `sqrt(ChiSquared)`, so tried staging the underlying ChiSquared/Gamma samples into the destination and applying scalar or vector `sqrt` as a bulk transform | Added `zig build chi-probe`. Local rows: current fill about 140M FastPrng and 124M ScalarPrng; staged vector sqrt about 129M FastPrng and 116M ScalarPrng, with matching checksums. | Rejected as a default path. The current per-sample path keeps Gamma generation and square root tightly coupled; staging loses more than vector `sqrt` recovers. |
| Pareto bulk fill APIs | `rand_distr pareto`: about 25M samples/s | Added `fillPareto`, `fillParetoFrom`, and `Pareto.fill` / `fillFrom` for high-volume slices. Initial rows were about 36M facade fill and 38M direct-source fill. A follow-up `pareto-probe` showed that staging strict-open uniforms and applying vector `exp(-log(u) / shape)` transforms lifts bulk throughput; latest rows are about 66M facade fill and about 65M direct-source fill. | Adopted for API consistency, allocation-free bulk workflows, and a large high-volume speedup over local Rust evidence. |
| Pareto shape=1 reciprocal fill path | `Pareto(scale, 1)` can be transformed as `scale / U`; the bulk path previously used the generic vector `exp(log(u) * -inverse_shape)` transform | Extended `pareto-probe` and routed `fillParetoFrom(shape == 1)` through strict-open uniforms plus reciprocal scaling. Local fill rows improved from about 57M to 475M FastPrng and from about 56M to 422M ScalarPrng. The single-sample shape=1 reciprocal branch was not adopted because it risked codegen regressions in the ordinary single-sample shape=3 path; ordinary shape=3 fill stayed in range at about 67M FastPrng and 66M ScalarPrng. | Adopted narrowly for parameterized bulk fill. Keep single-sample Pareto on the generic expression shape until a no-regression specialization is found. |
| Weibull bulk fill APIs | `rand_distr weibull`: about 16M samples/s | Added `fillWeibull`, `fillWeibullFrom`, and `Weibull.fill` / `fillFrom` for high-volume slices. Initial rows were about 23M facade fill and 24M direct-source fill. A follow-up `weibull-probe` showed that staging strict-open uniforms and applying vector `exp(log(-log(u)) / shape)` transforms lifts bulk throughput; latest rows are about 42M facade fill and about 41M direct-source fill. | Adopted for API consistency, allocation-free bulk workflows, and a large high-volume speedup over local Rust evidence. |
| Weibull shape=1 exponential fast path | `Weibull(scale, 1)` is exactly exponential with mean `scale`, but Alea previously used the generic `pow(-log(u), 1 / shape)` transform | Extended `weibull-probe` and routed `weibullFrom(shape == 1)` / `fillWeibullFrom(shape == 1)` through `scale * standardExponential`. Local rows improved single-sample from about 130M to 466M FastPrng and from about 151M to 579M ScalarPrng; fill improved from about 42M to 392M FastPrng and from about 41M to 453M ScalarPrng. Ordinary `Weibull(2, 1.5)` rows stayed in range: about 24M single-shot and 41M fill. `distcheck` now includes a `weibull-shape-one` mean gate. | Adopted narrowly for parameterized shape=1 APIs. Generic shape paths remain on the vectorized transform. |
| Gumbel bulk fill APIs | Local Rust evidence uses repeated sampler calls | Added `fillGumbel`, `fillGumbelFrom`, and `Gumbel.fill` / `fillFrom` for high-volume slices. Initial rows were about 62M facade fill and 68M direct-source fill. A follow-up `gumbel-probe` showed that staging open-closed uniforms and applying vector double-log transforms lifts bulk throughput; latest rows are about 86M facade fill and about 85M direct-source fill. | Adopted for API consistency, allocation-free bulk workflows, and a measurable bulk speedup over local Rust evidence. |
| Frechet bulk fill APIs | Local Rust evidence uses repeated sampler calls | Added `fillFrechet`, `fillFrechetFrom`, and `Frechet.fill` / `fillFrom` for high-volume slices. Initial rows were about 26M facade fill and 27M direct-source fill. A follow-up `frechet-probe` showed that staging open-closed uniforms and applying vector `exp(log(-log(u)) / -shape)` transforms lifts bulk throughput; latest rows are about 47M facade fill and about 47M direct-source fill. | Adopted for API consistency, allocation-free bulk workflows, and a measurable high-volume speedup over scalar repeated sampling. |
| Frechet shape=1 reciprocal-log fill path | `Frechet(location, scale, 1)` can be transformed as `location - scale / log(U)`. A faster `scale / Exp(1)` candidate changes the random stream shape, while reciprocal-log preserves the existing open-closed uniform transform | Extended `frechet-probe` and routed `fillFrechetFrom(shape == 1)` through vector reciprocal-log conversion after filling open-closed uniforms. Local fill rows improved from about 40M to 193M for both FastPrng and ScalarPrng, with matching checksums versus the existing transform. The faster reciprocal-exponential candidate was rejected for now because it changes stream shape; ordinary `Frechet(shape=3)` fill stayed in range at about 47M. | Adopted narrowly for parameterized bulk fill. Keep single-sample Frechet and reciprocal-exponential variants out of default paths until stream-shape/reproducibility tradeoffs are explicitly accepted. |
| PERT bulk fill APIs | Local Rust evidence uses repeated sampler calls | Added `fillPert`, `fillPertFrom`, and `Pert.fill` / `fillFrom` for high-volume slices. Local rows: facade fill about 67M samples/s and direct-source fill about 77M, above scalar facade repeated sampling around 70M in this run. | Adopted for API consistency and allocation-free bulk workflows; direct-source fill is a useful high-volume path. |
| PERT shape=0 uniform-equivalent audit | `PERT(min, mode, max, shape=0)` reduces to `Uniform(min, max)`, so checked whether it needed a dedicated path beyond the `Beta(1,1)` specialization | Added `zig build pert-probe`. Single-sample rows already match uniform: about 951M FastPrng and 1250M ScalarPrng. Bulk fill is mixed: uniform fill is faster for FastPrng, about 834M versus current 671M, but slower for ScalarPrng, about 685M versus current 819M. Checksums match. | No production change for now. The current Beta-backed fill remains the better generic default because `Rng`/direct-source callers may use ScalarPrng; revisit only with engine-aware or no-regression fill dispatch. |
| Standard normal/exponential reusable samplers | Rust exposes `StandardNormal` / `Exp1` fast-path distribution types and local rows are about 466M / 452M samples/s | `StandardNormal(T)` / `StandardExponential(T)` added; after raw standard helpers, ratio-threshold normal, and mantissa-threshold exponential fast paths, scalar direct rows are about 449M standard normal, 439M f32 standard normal, about 431M standard exponential, and about 422M f32 standard exponential on the current Linux host | Adopted for feature parity and scalar-fast workflows; still watch the remaining raw ziggurat gap |
| Standard normal/exponential bulk fill APIs | Rust exposes zero-parameter distribution types and users often fill repeated standard samples | Added `fillStandardNormal`, `fillStandardNormalFrom`, `fillStandardExponential`, `fillStandardExponentialFrom`, and sampler `fill` / `fillFrom` methods. After the exponential mantissa-threshold fast path, `standard-fill-probe` shows the fill helpers match direct loops in isolation: about 466M standard normal and about 442M standard exponential. Full throughput rows remain lower because of benchmark/context loop shape rather than a separate helper dispatch cost. | Adopted for explicit zero-parameter bulk workflows; keep production fill helpers as-is. |
| Route standard normal/exponential fill through `Rng` raw standard helpers | Tried adding `Rng.fillStandardNormalFrom` / `fillStandardExponentialFrom` and routing distribution-layer standard fills through those helpers | Rejected after benchmark: `fillStandardNormal scalar direct` dropped from the recent 400M range to about 289M samples/s; exponential was roughly neutral around 369M. | Rejected. Keep the distribution-layer fill loops calling `standardNormalFrom` / `standardExponentialFrom` directly until a better bulk strategy is found. |
| Normal/Exponential reusable sampler fill APIs | `Normal(T)` / `Exponential(T)` reusable samplers had `sampleFrom` but lacked `fill` / `fillFrom`, unlike most other reusable samplers | Added `Normal(T).fill` / `fillFrom` and `Exponential(T).fill` / `fillFrom`, with unit coverage and benchmark rows. Current rows track the direct fill helpers: `Normal.fillFrom scalar direct` remains around the direct normal fill profile, while `Exponential.fillFrom scalar direct` is about 388M after the exponential mantissa-threshold fast path. A later `Exponential.fillFrom` reroute through `Rng.fillExponentialFrom(..., rate=1)` plus scaling measured about 369M on `zig build -Doptimize=ReleaseFast -Dcpu=native bench -- 16777216`, below `fillExponential scalar direct` around 387M. | Adopted for API consistency and allocation-free reusable-sampler workflows; keep the direct per-sample `sampleFrom` loop for `Exponential.fillFrom` because the staged scaling reroute regresses the scalar-direct fill row. |
| Beta/Fisher reusable samplers | `rand_distr beta`: about 16M samples/s | `alea beta`: about 77M single-shot, about 87M cached; `alea fisher-f`: about 72M facade, about 80M direct-source/cached | Closed for current local Rust evidence; cached/direct Gamma composition is substantially faster |
| Beta(1,1) uniform fast path | `Beta(1,1)` is exactly `Uniform(0,1)`, but Alea previously sampled two Gamma variates and normalized them | Added `zig build beta-special-probe` and routed parameterized `betaFrom(1,1)` / `fillBetaFrom(1,1)` through `Rng.floatFrom` / `Rng.fillFrom`. A follow-up routed reusable `Beta(1,1).sampleFrom/fillFrom` through matching method tags instead of the Gamma-backed sampler path. Local parameterized rows improved single-sample `Beta(1,1)` from about 237M to 1140M FastPrng and from about 289M to 1505M ScalarPrng; fill improved from about 216M to 873M FastPrng and from about 130M to 706M ScalarPrng. Reusable `Beta(1,1).sampleFrom` improved from about 241M to 868M FastPrng and from about 302M to 1298M ScalarPrng, with fill now around 597M/722M. Ordinary `Beta(2,5)` rows stayed in range: about 72M facade, 82M cached, 73M fill, and 84M scalar fill. `distcheck` includes `beta-unit`. | Adopted for parameterized and reusable edge APIs. Cached `Beta(2,5)` remains on the Gamma-backed sampler path. |
| Beta(alpha,1) inverse-CDF fast path | `Beta(alpha,1)` can be sampled as `U^(1/alpha)`, and the common `Beta(2,1)` case reduces to `sqrt(U)` | Extended `beta-special-probe` and routed parameterized `betaFrom(alpha,1)` / `fillBetaFrom(alpha,1)` through inverse-CDF sampling, with a dedicated `sqrt` branch for `alpha == 2`. Local `Beta(2,1)` rows improved single-sample from about 115M to 664M FastPrng and from about 135M to 664M ScalarPrng; fill improved from about 112M to 316M FastPrng and from about 132M to 348M ScalarPrng. The opposite `Beta(1,5)` inverse-CDF candidate was rejected because it dropped to about 44-46M versus current 119-143M single-sample rows. Ordinary `Beta(2,5)` stayed in range. `distcheck` now includes a `beta-alpha-two` mean gate. | Adopted narrowly for parameterized APIs. Cached `Beta(2,5)` remains on the Gamma-backed sampler path; do not use the slower `Beta(1,beta)` complement-root form as a default. |
| Beta(1,2) complement-sqrt fast path | `Beta(1,2)` can be sampled as `1 - sqrt(U)`, a cheap special case distinct from the slower generic `Beta(1,beta)` complement-root form | Extended `beta-special-probe` and routed parameterized `betaFrom(1,2)` / `fillBetaFrom(1,2)` through the complement-sqrt form. Reusable `Beta(1,1)`, `Beta(2,1)`, and `Beta(1,2)` now share method-tag dispatch for these edge cases. Local rows improved single-sample from about 116M to 663M FastPrng and from about 139M to 663M ScalarPrng; fill improved from about 73M to 316M FastPrng and from about 76M to 348M ScalarPrng. The generic `Beta(1,5)` complement-root candidate remains rejected because it is slower than the current Gamma-backed path. `distcheck` includes `beta-one-two`. | Adopted for parameterized and reusable edge APIs. Do not generalize this to arbitrary `Beta(1,beta)` without stronger evidence. |
| Beta bulk fill APIs | Local Rust evidence uses repeated sampler calls | Added `fillBeta`, `fillBetaFrom`, and `Beta.fill` / `fillFrom` for high-volume Beta slices. Local rows: facade fill about 73M samples/s and direct-source fill about 84M, above single-shot facade sampling around 71M and close to cached repeated sampling around 81M. | Adopted for API consistency and allocation-free bulk workflows; direct-source fill is a useful high-volume path. |
| ChiSquared/StudentT reusable samplers | `rand_distr chi-squared`: about 125M samples/s; `rand_distr student-t`: about 104M | `alea chi-squared`: about 137M single-shot, about 159M cached; `alea student-t`: about 104M facade, about 123M cached, about 125M direct-source after `studentTFrom` | Closed for current local Rust evidence; single-shot `studentT` now routes through direct-source composition |
| Fisher-F and Student-T bulk fill APIs | `rand_distr fisher-f`: about 66M; `rand_distr student-t`: about 104M | Added `fillFisherF`, `fillFisherFFrom`, `fillStudentT`, `fillStudentTFrom`, and reusable sampler `fill` / `fillFrom` methods. Local rows: Fisher-F fill about 73M samples/s and direct-source fill about 84M; Student-T fill about 96M and direct-source fill about 113M, below cached repeated sampling around 122M. | Adopted for API consistency and allocation-free bulk workflows; Fisher-F and Student-T direct-source fills exceed current local Rust evidence. |
| FisherF(2,2) uniform-ratio candidate | `FisherF(2,2)` is distribution-equivalent to the ratio of two exponentials and to `U / (1 - U)`, so tested whether the parameterized path should use the cheaper uniform ratio | Added `zig build fisher-f-probe`. The uniform-ratio candidate is faster, about 914M FastPrng and 996M ScalarPrng single-sample versus current 224M/274M, and about 475M/422M fill versus current 217M/130M. Checksums differ substantially because the random stream shape changes. | Deferred/rejected as a default path for now. Keep FisherF on Gamma-ratio composition until stream-shape/reproducibility tradeoffs are explicitly accepted or a separate opt-in path is designed. |
| StudentT dof=1 Cauchy-equivalent candidate | `StudentT(1)` is distribution-equivalent to standard Cauchy, so tested whether `studentTFrom(dof == 1)` / `fillStudentTFrom(dof == 1)` should route through Cauchy | Added `zig build student-t-probe`. Cauchy-equivalent fill is faster, about 75M FastPrng and 73M ScalarPrng versus current StudentT fill around 31M/34M, but single-sample Cauchy is much slower at about 62M/68M versus current StudentT around 230M/273M, and checksums differ because the random stream shape changes. | Deferred/rejected as a default path for now. Keep StudentT on its current normal/chi-squared composition until the stream-shape tradeoff is explicitly accepted or a no-regression fill-only API is designed. |
| ChiSquared bulk fill APIs | Local Rust evidence uses repeated sampler calls | Added `fillChiSquared`, `fillChiSquaredFrom`, and `ChiSquared.fill` / `fillFrom` for high-volume slices. Local rows: facade fill about 134M samples/s and direct-source fill about 152M, aligned with cached Gamma-backed sampling. | Adopted for API consistency and allocation-free bulk workflows; not a speed win over cached repeated sampling. |
| ChiSquared dof=1 normal-square fill path | `ChiSquared(1)` is exactly `N(0,1)^2`. Single-shot `chiSquaredFrom(dof=1)` already benefits from the `Gamma(0.5, 2)` fast path, but `fillChiSquaredFrom(dof=1)` still used the reusable Gamma-backed sampler | Extended `chi-probe` and routed `fillChiSquaredFrom(dof == 1)` through a direct normal-square loop. Local rows improved fill from about 34M to 422M FastPrng and from about 37M to 477M ScalarPrng, while ordinary `dof=4` rows stayed in range: about 140M single-shot, 158M cached, 137M fill, and 156M scalar fill. `distcheck` now includes a `chi-squared-dof-one` mean gate. | Adopted narrowly for the parameterized fill API. Cached `ChiSquared(dof=4)` remains on the Gamma-backed sampler path. |
| ChiSquared dof=2 exponential-equivalent audit | `ChiSquared(2)` is exactly `2 * Exp(1)`, so checked whether it still needed a dedicated path after the `Gamma(1, scale)` specialization | Extended `chi-probe`. Local rows already match the explicit exponential-equivalent path: about 476M FastPrng and 576M ScalarPrng for single samples, and about 390M FastPrng / 450M ScalarPrng for fill. Checksums match the `2 * standardExponential` candidate. | No production change needed. `ChiSquared(2)` already reaches the equivalent path through `Gamma(1, 2)` / `fillGammaFrom`; keep this as audit evidence to avoid duplicating code. |
| Chi bulk fill APIs | Local Rust evidence uses repeated sampler calls | Added `fillChi`, `fillChiFrom`, and `Chi.fill` / `fillFrom` for high-volume slices. Local rows: facade fill about 132M samples/s and direct-source fill about 150M, close to cached ChiSquared-backed sampling around 155M. | Adopted for API consistency and allocation-free bulk workflows; not a speed win over cached repeated sampling. |
| Chi dof=1 absolute-normal fast path | `Chi(1)` is exactly `abs(N(0,1))`. The generic path computed `sqrt(ChiSquared(1))`, and bulk fill still used the reusable sampler path | Extended `chi-probe` and routed `chiFrom(dof == 1)` / `fillChiFrom(dof == 1)` through direct absolute-normal sampling. Local rows improved single-sample `Chi(1)` from about 468M to 503M FastPrng and from about 539M to 633M ScalarPrng; fill improved from about 34M to 419M FastPrng and from about 36M to 482M ScalarPrng. Ordinary `Chi(4)` fill stayed in range: about 136M facade and 154M scalar direct. `distcheck` now includes a `chi-dof-one` mean gate. | Adopted narrowly for parameterized `Chi(1)` single-shot and fill APIs; cached `Chi(dof=4)` remains on the ChiSquared-backed sampler path. |
| Chi dof=2 Rayleigh-equivalent audit | `Chi(2)` is Rayleigh with scale 1, and also `sqrt(2 * Exp(1))`. Checked whether it needed a dedicated path after the `ChiSquared(2)` / `Gamma(1,2)` specialization | Extended `chi-probe`. Current `Chi(2)` already matches the exponential-backed Rayleigh candidate with matching checksums: about 449M FastPrng and 512M ScalarPrng for single samples, and about 375M/413M for fill. The strict-open Rayleigh transform is slower and has a different checksum, about 153M/172M single-sample and 198M/188M fill. | No production change needed. `Chi(2)` already reaches the faster equivalent path through `ChiSquared(2)`; keep this as audit evidence and do not route it through the slower Rayleigh default. |
| Erlang bulk fill APIs | Local Rust evidence uses repeated sampler calls | Added `fillErlang`, `fillErlangFrom`, and `Erlang.fill` / `fillFrom` for high-volume slices. Local rows: facade fill about 137M samples/s and direct-source fill about 157M, close to cached Gamma-backed sampling around 163M. | Adopted for API consistency and allocation-free bulk workflows; not a speed win over cached repeated sampling. |
| Erlang small-k product/vector-log candidate | For integer shape `k`, Erlang can be sampled as `-scale * log(product(open_uniform[0..k]))`, so tried staging open uniforms and applying vector `log` for small shapes | Added `zig build erlang-probe`. On a 33.5M-sample run, FastPrng-backed facade improved for `k=2` from about 144M to 180M and for `k=3` from about 148M to 155M, but `k=4` dropped from about 150M to 136M. ScalarPrng-backed facade was neutral/slower for `k=2` at about 163M vs 165M and much slower for `k=3` at about 137M vs 170M; direct ScalarPrng `k=3` was also slower at about 138M vs 168M. | Rejected as a default path. The gain is too engine- and shape-specific for the generic `Rng`/direct-source API, which cannot assume a FastPrng-backed facade without regressing scalar-heavy workflows. |
| LogNormal reusable sampler | `rand_distr log-normal`: about 149M samples/s | `alea log-normal`: about 104M with `FastPrng`; about 119M with reusable `LogNormal.sampleFrom(ScalarPrng)` | Direct source path helps but still trails Rust |
| InverseGaussian/NormalInverseGaussian reusable samplers | `rand_distr inverse-gaussian`: about 77M; `rand_distr normal-inverse-gaussian`: about 67M | `alea inverse-gaussian`: about 69M single-shot, about 75M cached; `alea normal-inverse-gaussian`: about 60M single-shot, about 63-65M cached/direct | Watch only for scalar single-shot/cached rows; bulk paths below now exceed local Rust evidence. |
| NormalInverseGaussian bulk fill APIs | Local Rust evidence uses repeated sampler calls | Added `fillNormalInverseGaussian`, `fillNormalInverseGaussianFrom`, and `NormalInverseGaussian.fill` / `fillFrom` for high-volume slices. Initial local rows were about 47M facade fill and 60M direct-source fill. After vectorized InverseGaussian fill, `NormalInverseGaussian.fillFrom` stages inverse-Gaussian values and then combines with scalar normals. Latest `nig-probe -- 4194304` rows are about 152.5M current FastPrng fill versus 155.4M staged scalar combine, and about 168.5M current ScalarPrng fill versus 167.0M staged scalar combine; staged vector4 combine trails both profiles. | Adopted for API consistency, allocation-free bulk workflows, and a large high-volume speedup over local Rust evidence. Keep current combine shape until an engine-agnostic no-regression variant is found. |
| InverseGaussian bulk fill APIs | Local Rust evidence uses repeated sampler calls | Added `fillInverseGaussian`, `fillInverseGaussianFrom`, and `InverseGaussian.fill` / `fillFrom` for high-volume slices. Initial rows were about 66M facade fill and 70M direct-source fill. `inverse-gaussian-probe` showed that staging standard normals then vectorizing the Michael-Schucany-Haas transform with lane-wise `sqrt`/`select` reaches about 235M facade and 250M scalar-direct in isolation; a fresh `inverse-gaussian-probe -- 4194304` run shows current production fill about 239M FastPrng and 258M ScalarPrng, slightly ahead of the staged vector4 candidate at about 239M/257M and far ahead of staged scalar around 80-82M. | Adopted. Bulk InverseGaussian now substantially exceeds local Rust evidence while single-sample APIs retain the scalar draw order. |
| SkewNormal scalar profile | `rand_distr skew-normal`: about 263M samples/s | `alea skew-normal`: about 186M with `FastPrng`; about 229M with reusable `SkewNormal.sampleFrom(ScalarPrng)` | Watch: direct source path helps, but still trails Rust |
| SkewNormal general-shape benchmark evidence | Added `shape = 2` rows to cover the generic linear-combination branch instead of only the `shape = 1` max-normal branch. Local Rust `shape=2` row is about 204M samples/s. | Added Alea rows: facade `shape=2` about 173M, reusable scalar direct about 171M, and reusable scalar fill evidence. | Evidence only; generic-shape SkewNormal also trails local Rust and remains tied mostly to the two-normal hot path. |
| Unit geometry scalar profile | `rand_distr unit circle/sphere`: about 159M samples/s; unit ball about 56M | `alea unit circle/disc/sphere`: about 119-128M with `FastPrng`, about 150-162M with `ScalarPrng` direct; unit ball about 59M direct | Mostly closed for scalar-fast profile; `FastPrng` facade still trails Rust surface/sphere rows |
| Alphanumeric strings | `rand alphanumeric`: about 840M chars/s | `alea alphanumeric`: about 949M chars/s | Closed for current local Rust evidence |

## Rejected Or Deferred Attempts

| Attempt | Result | Decision |
| --- | --- | --- |
| Lower Poisson PTRS threshold from `lambda >= 30` to `lambda >= 12` | `lambda = 20` benchmark dropped from about 26M samples/s to about 23M samples/s, while `distcheck` still passed | Rejected. Dedicated medium-lambda Ahrens-Dieter path was adopted instead. |
| Cache Poisson Ahrens-Dieter constants in reusable `Poisson` sampler | `alea poisson cached`: about 52M samples/s, essentially same as single-shot after adoption | Kept for API quality and avoiding recomputation, but not enough to close the remaining Rust performance gap. |
| Direct `normalFrom` helper wrapping `std.Random.floatNorm` | About 208M samples/s, essentially same as facade/direct `std.Random` and still far below `rand_distr` | Rejected as public API. Normal gap needs a faster normal kernel, not another wrapper. |
| SplitMix64 as scalar-fast distribution engine | `splitmix64 next`: about 1980M next/s; `normal splitmix64 direct`: about 375M samples/s | Rejected as scalar-fast profile. `Wyhash64` remains better for scalar-heavy distribution workloads. |
| Force-inline `Wyhash64.next` / `wymix` | No durable benchmark win: standard-normal stayed around 426M samples/s, standard-exponential around 430M, with several derived rows flat or slightly lower | Rejected. Keep engine declarations simple and focus on algorithm-level or compiler-visible call-shape changes. |
| Direct ziggurat normal using engine `next()` instead of `std.Random.int` | About 386-390M samples/s; repeated runs showed `std.Random.floatNorm` can also fall back near 210M | Adopted as default. Still trails `rand_distr`, so keep optimizing. |
| Reusable `Normal(T)` sampler switched from cached Box-Muller to direct ziggurat | `alea log-normal` improved from about 47M to about 104M samples/s and normal-derived reusable samplers benefit | Adopted. Keep Box-Muller only as a possible future specialized sampler if needed. |
| Reusable/direct `LogNormal(T).sampleFrom` and `logNormalFrom` | `alea log-normal scalar direct`: about 119M samples/s versus the facade/cached row around 104M | Adopted. This carries scalar-fast normal sampling into log-normal workflows. |
| LogNormal bulk fill APIs | Local Rust evidence uses repeated sampler calls | Added `fillLogNormal`, `fillLogNormalFrom`, and `LogNormal.fill` / `fillFrom` for high-volume log-normal slices. Initial rows were about 101M samples/s for facade fill and 112M for direct-source fill. A follow-up `log-normal-probe` showed that staging normals into the destination and applying vector `@exp` in place beats per-sample `normal + exp`; production fill now uses that staged path. Latest local rows are about 127M facade fill and about 133M direct-source fill. | Adopted for API consistency, allocation-free bulk workflows, and a measurable bulk speedup; scalar direct repeated sampling still remains below local Rust. |
| HalfNormal bulk fill APIs | Local Rust evidence uses repeated sampler calls | Added `fillHalfNormal`, `fillHalfNormalFrom`, and `HalfNormal.fill` / `fillFrom` for high-volume slices. Local rows: facade fill about 298M samples/s and direct-source fill about 338M samples/s, below scalar direct repeated sampling around 410M. | Adopted for API consistency and allocation-free bulk workflows; not a speed win yet. |
| HalfNormal staged abs transform | Tried staging normal samples into the destination then applying scalar/vector absolute-value transforms in place | Latest `half-normal-probe -- 4194304` shows staged vector abs now slightly wins the FastPrng profile, about 380M versus current 374M, but still regresses the ScalarPrng profile, about 425M versus current 440M. | Still rejected as a default path because it regresses the scalar-fast profile. Keep the current per-sample path until a no-regression staged variant is found. |
| LogNormal via `exp2(x * log2e)` | `alea log-normal scalar direct` dropped from about 119M to about 101M samples/s | Rejected. Keep direct `@exp` for log-normal transformation. |
| LogNormal direct standard-normal expression | Replaced single-sample `logNormalFrom` / reusable `LogNormal.sampleFrom` with `@exp(mean + stddev * standardNormalFastFrom(...))` while keeping the staged bulk fill path unchanged. A fresh `log-normal-probe -- 4194304` still shows the explicit expression ahead in isolation, about 134M versus the old current shape around 127M. Full native bench after adoption reports `alea log-normal scalar direct` about 124M and `fillLogNormal scalar direct` about 134M, with fill rows still governed by the staged production fill shape. | Adopted narrowly for single-sample APIs. Keep current staged/vectorized fill path; do not switch fill rows to the slower staged/vector exp candidates from the probe. |
| Direct ziggurat exponential using engine `next()` instead of `std.Random.int` | About 383M samples/s, essentially same as previous stdlib-backed path | Adopted internally for consistency with normal; not enough to close the Rust performance gap. |
| Distribution-layer normal/exponential via `std.Random.floatNorm` / `floatExp` | Older `distributions.normal` and reusable `Exponential.sample` paths used `std.Random`-backed sampling even after `Rng.normal`/`Rng.exponential` had direct ziggurat fast paths | Replaced with direct fast paths and added standard reusable samplers for explicit zero-parameter workflows. |
| Route all non-`Rng` `normalFastFrom` / `exponentialFastFrom` calls through `source.random()` | `normal wyhash64 std.Random` is about 245M samples/s, while `normal wyhash64 direct` is about 427M samples/s | Rejected as a generic rule. Keep direct ziggurat fast path; use explicit benchmarked bulk helpers for ScalarPrng recommendations. |
| Direct and checked discrete sampler methods | `Bernoulli`, `Binomial`, `NegativeBinomial`, `Hypergeometric`, `Geometric`, and `Poisson` now expose `sampleFrom`; top-level Bernoulli, Binomial, NegativeBinomial, Hypergeometric, Geometric, GeometricFailures, and Poisson helpers have direct and checked variants | Adopted for API consistency, validated user-supplied parameters, and direct-engine workflows. Keep integer-threshold Bernoulli/binomial paths to avoid float-comparison regressions. |
| Direct and checked binomial Poisson approximation | `binomialPoissonApprox` remained the only public parameterized distribution helper without direct-source or checked variants | Added `binomialPoissonApproxFrom`, `binomialPoissonApproxChecked`, and `binomialPoissonApproxCheckedFrom`, preserving the existing approximation semantics while validating user-supplied probabilities. | Adopted for API consistency and direct-engine workflows; this is not a new exact-binomial path. |
| Rand-style geometric failure-count API | Local `rand_distr::Geometric` returns the number of failures before first success, while Alea's original `Geometric` returns the one-based trial count | Added `geometricFailures`, `geometricFailuresFrom`, `fillGeometricFailures`, `fillGeometricFailuresFrom`, and `GeometricFailures` reusable sampler without changing existing `Geometric` semantics. | Adopted for compatibility with `rand_distr` semantics and to avoid a silent off-by-one migration trap. |
| StandardGeometric p=0.5 fast path | Local `rand_distr::StandardGeometric` uses repeated `leading_zeros()` on `u64` words | Added `standardGeometric`, `standardGeometricFrom`, `fillStandardGeometric`, `fillStandardGeometricFrom`, and `StandardGeometric`. Local rows: facade about 748-758M samples/s, scalar direct about 1263M, and direct-source fill about 994M, compared with Rust around 913M. | Adopted for rand_distr semantic coverage and high-volume p=0.5 failure-count sampling. |
| Discrete bulk fill APIs | Core discrete distributions only exposed single-sample helpers or reusable sampler calls | Added distribution-layer `fill` / `fillFrom` APIs for Bernoulli, Binomial, NegativeBinomial, Hypergeometric, Poisson, and Geometric. Bernoulli fill reuses packed `Rng.fillChanceFrom` for p=1/2 and p=1/4. Local rows: `fillBernoulli p=0.25` about 2.83B samples/s; direct-source fills are about 68M Poisson, 128M GeometricFailures, 126M one-based Geometric, 49M Binomial, 25M NegativeBinomial, and 23M Hypergeometric samples/s. | Adopted for API consistency, allocation-free bulk workflows, and packed Bernoulli coverage; direct-source fills are useful high-volume paths, while facade fills mostly track repeated cached sampling. |
| Hypergeometric HIN inverse-transform fast path | Local Rust uses HIN/H2PE algorithms and outperformed Alea's simple draw loop on `(100, 30, 10)` | Added a cached HIN inverse-transform method for parameter regimes where the mode is close to the lower bound, keeping the draw-loop fallback elsewhere. Local row improved from about 20M to about 51M samples/s; fill rows are about 48-49M. | Adopted. This closes the current benchmarked Hypergeometric gap without taking on full H2PE complexity yet. |
| Hypergeometric H2PE port attempt | Tried porting local `rand_distr`'s rejection/acceptance H2PE path for large-parameter Hypergeometric workloads | The initial production port was rejected after the full benchmark showed no large-row win. A later isolated `hypergeo-h2pe-probe` iteration reached about 24.8M samples/s and was then integrated into production. | Superseded by the adopted production H2PE path. |
| Hypergeometric H2PE production path | Large-parameter Hypergeometric was still using the slow draw-loop path | Added H2PE rejection/acceptance sampling for large-mode regimes while keeping HIN for small-mode regimes. Local rows: small `(100,30,10)` remains around 50-51M samples/s; large `(5000,2500,500)` improves from about 0.3M to about 20.7M facade/fill and about 23.1M direct-source fill. | Adopted. This closes the current large Hypergeometric benchmark gap without regressing the HIN row. |
| Direct reusable continuous/tail sampler methods | Several reusable continuous and tail samplers previously only exposed `sample(rng)` | `Uniform`, `Triangular`, `Cauchy`, `Pareto`, `Weibull`, `Gumbel`, `Frechet`, `Pert`, `Zipf`, and `Zeta` now expose `sampleFrom`; matching single-shot `From` helpers were added where absent. Representative local rows: triangular direct about 216M vs 188M facade, gumbel direct about 72M vs 66M facade, zipf direct about 15.4M vs 14.9M facade. | Adopted for API consistency and direct-engine workflows; benchmark representative paths instead of duplicating every distribution row. |
| Zipf/Zeta reusable sampler fill APIs | `Zipf(T)` and `Zeta(T)` had direct `sampleFrom` but no reusable bulk fill methods | Added `Zipf(T).fill` / `fillFrom` and `Zeta(T).fill` / `fillFrom`, with unit coverage and benchmark rows. Local rows: `Zipf.fillFrom direct` about 15.5M samples/s, matching `zipf direct`; `Zeta.fillFrom direct` about 34.9M, near `zeta direct` about 35.7M. | Adopted for API consistency and allocation-free tail-distribution bulk workflows; performance tracks existing direct reusable sampling. |
| Direct-source float basis helpers | Distribution-layer direct helpers duplicated open/open-closed float construction | `Rng.floatOpenFrom`, `floatOpenClosedFrom`, and `floatRangeFrom` are public direct-source helpers; distribution helpers now reuse them instead of local private copies | Adopted for API consistency and for future distribution/SIMD work; this is a foundation cleanup, not a standalone speed claim. |
| Scalar open/open-closed float fill APIs | Strict open/open-closed float bulk generation required `fillSample(Open01)` or a manual loop | Added `fillOpen`, `fillOpenFrom`, `fillOpenClosed`, and `fillOpenClosedFrom`. Local rows: `fillOpen f32` about 759M samples/s and `fillOpenClosed f32` about 777M samples/s, versus ordinary `fill f32` about 839M samples/s. | Adopted for high-volume strict-interval workflows; slower than ordinary `[0, 1)` fill because it preserves stronger interval semantics. |
| `f32` uniform vector and bulk fill path | `fill(f32)`, `fillOpen(f32)`, and `fillOpenClosed(f32)` used scalar slice loops even though vector `f32x8` generation had stronger throughput | Bulk `f32` fill/open/open-closed paths now generate `@Vector(8, f32)` chunks and keep scalar tails. Local rows: `fill f32` improved from about 844M to about 1158M samples/s, `fillOpen f32` from about 760M to about 1146M, and `fillOpenClosed f32` from about 785M to about 1136M. | Adopted as SIMD/vector foundation work for high-volume uniform float slices. |
| Vector open/open-closed float helpers | Vector APIs had `[0, 1)` float generation but no direct open or open-closed vector counterparts | Added `vectorOpen`, `vectorOpenClosed`, `vectorOpenFrom`, `vectorOpenClosedFrom`, and vector-slice fill helpers for float vectors. Packed `f32` lane generation avoids the initial scalar-per-lane cost and fixes strict-open `f32` zero/NaN edge cases. `vectorbench` rows are about 1.04B lanes/s for open/open-closed `f32x8`, roughly matching ordinary range `f32x8`. | Adopted as SIMD/vector API coverage and a foundation for distribution kernels that need strict open intervals. |
| `f64` uniform vector and bulk fill path | `f64` vector uniform/open/open-closed/range generation previously used scalar-per-lane construction, and main throughput rows only tracked `f32` float fill/range | Added vectorized `f64` lane conversion for `vector`, `vectorOpen`, and `vectorOpenClosed`, use it for `fill f64`, `fillOpen f64`, `fillOpenClosed f64`, and `fillRange f64`, and added `bench`/`vectorbench` rows. Local rows: `fill f64` about 595M samples/s, `fillOpen f64` about 572M, `fillOpenClosed f64` about 579M, `fillRange f64` about 517M; `vectorbench` shows `f64x4` open about 523M lanes/s versus the pre-change scalar-lane baseline about 503M. | Adopted as SIMD/vector foundation work and benchmark coverage for `f64` uniform bulk paths; distribution-kernel gaps remain open. |
| f64 bitcast uniform generation | f64 `[0, 1)` generation used integer-to-float conversion and multiplication, trailing Rust scalar float evidence | Switched ordinary `f64` uniform and vector/range f64 paths to exponent-bit construction. Local rows: `fill f64` improved from about 596M to about 884M samples/s, `fillRange f64` from about 569M to about 827M, `fillVector f64x4` from about 543M to about 722M lanes/s, and `fillVectorRange f64x4` from about 523M to about 695M. | Adopted. This changes f64 bulk stream mapping, which is allowed by the reproducibility matrix for non-byte fill policy; open/open-closed f64 still use stronger endpoint semantics and remain watch. |
| 53-bit f64 bitcast uniform attempt | Tried to preserve the 53-bit `n / 2^53` grid by adding the discarded low precision bit back as a half-ULP after exponent-bit construction | `fill f64` dropped from about 884M to about 729M samples/s, `fillRange f64` dropped from about 827M to about 688M, and scalar direct rows also fell below Rust float/range evidence. | Rejected. Keep the fast 52-bit exponent-bit bulk path for now; revisit only with a faster 53-bit construction. |
| f64 Open01 bitcast generation | f64 strict-open `(0, 1)` used integer-to-float conversion and multiplication, trailing Rust `Open01 f64` evidence | Switched f64 Open01 scalar/vector paths to exponent-bit construction with half-ULP offset. Local rows: `fillOpen f64` improved from about 568M to about 884M samples/s and `fillVectorOpen f64x4` from about 523M to about 722M lanes/s. | Adopted. Strict open-closed f64 still uses the multiply-plus-one path because it must include 1.0 exactly. |
| `fillOpenClosed(f64)` scalar loop | Vector `@floatFromInt` conversion made bulk `(0, 1]` f64 slower than scalar sampling | Replaced the vector-slice implementation with scalar `floatOpenClosedFrom` lane fill. Local row improved from about 581M to about 716M samples/s. | Superseded by the raw-word byte-fill conversion below; this remains useful history for why vector conversion is not used. |
| `fillOpenClosed(f64)` raw-word byte-fill conversion | The scalar loop still trailed Rust `OpenClosed01 f64` around 779M samples/s | For sources that expose byte `fill`, bulk generation now fills a temporary `u64` word buffer and applies the exact `(0, 1]` conversion from those raw words. Local row improved from about 713-716M to about 770M samples/s while keeping checksum/mean in the expected range. A fresh `zig build -Doptimize=ReleaseFast -Dcpu=native open-closed-probe -- 4194304` shows facade `fillOpenClosed` about 583.5M samples/s, direct `fillOpenClosedFrom` about 775.7M, scalar next conversion about 716.1M, and confirms the 128-word buffer remains best at about 776.5M versus 512-word about 770.3M and 2048-word about 764.1M. | Adopted. This keeps the current local `fillOpenClosed(f64)` bulk row at the Rust `OpenClosed01 f64` noise boundary while preserving exact `(0, 1]` semantics. |
| f64 OpenClosed01 bitcast attempt | Tried a bitcast-based `(0, 1]` implementation preserving 1.0 through a select branch | Scalar `floatOpenClosed f64` dropped from about 1040M to about 860M samples/s and bulk `fillOpenClosed f64` dropped from the scalar-loop row around 716M to about 593M. | Rejected. Keep the scalar-loop bulk path until a faster exact `(0, 1]` construction is found. |
| `fillOpenClosed(f64)` invalid byte-buffer read experiment | Tried filling a raw byte buffer and reading `u64` words out of it before conversion | The row appeared to jump to about 1.22B samples/s, but the checksum implied an invalid mean around 0.046 instead of near 0.5. | Rejected as invalid despite speed; benchmark checksums must stay in the expected distribution range. |
| `fillOpenClosed(f64)` in-place/raw-vector/unrolled conversions | Tried converting the destination memory in place, vectorizing the raw-word conversion, and manually unrolling the adopted temporary-word conversion | In-place conversion dropped to about 680M samples/s; vector/manual unrolled conversion dropped to about 685M; older scalar-loop unrolling dropped to about 579M. | Rejected. Keep the simple temporary `u64` word buffer loop until a faster exact `(0, 1]` construction is found. |
| Float range fill single-pass vector scaling | `fillRange(f32/f64)` generated `[0, 1)` values first and scaled the slice in a second pass | `fillRange(f32/f64)` now generates vector chunks and applies `min + width * uniform` immediately. Local rows: `fillRange f32` improved from about 691M to about 1069M samples/s; `fillRange f64` improved from about 517M to about 569M samples/s. | Adopted as bulk range/SIMD foundation work; scalar distribution gaps remain open. |
| Inline normal ziggurat tail helper | Tried marking `normalZigguratZeroCase` inline to match Rust's inlined zero-case closure shape | No visible normal/exponential win: normal direct rows stayed around 414-418M samples/s and exponential direct rows around 414-420M. This matches `ziggurat-stats`, where normal tail is only about 0.026%. | Rejected. Keep tail helper non-inline and focus on the hot ziggurat path. |
| Normal ziggurat tail open interval | Rust samples the normal ziggurat tail with `Open01`; Alea used ordinary half-open `floatFrom`, which could theoretically feed `log(0)` in a forced tail path | Switched `normalZigguratZeroCase` to `floatOpenFrom` and added a deterministic regression source that forces the zero-case with raw zero draws while still producing a finite result. Local normal hot-path rows are unchanged: `standard-normal scalar direct` remains about 449M samples/s. | Adopted as a correctness and Rust-semantics fix, not a speed change. |
| Probability bool bulk/vector sampling | No direct Rust row; fills a Zig-native API gap around repeated `chance(p)` workloads | Added `chanceFrom`, `fillChance`, `fillChanceFrom`, `vectorChance`/`vectorChanceFrom`, and vector-slice chance fills; p=0.25 uses packed two-bit generation at about 2.85B samples/s, 4.69B single-vector lanes/s, and 3.68B vector-slice lanes/s; p=0.5 uses packed bool generation around 3.0B samples/s and 9.8B lanes/s | Adopted for ergonomic high-volume probability sampling and vector API coverage. |
| Exact ratio bulk/vector sampling | No direct Rust row; fills a Zig-native API gap around repeated `ratio(n, d)` workloads | Added `ratioFrom`, `fillRatio`, `fillRatioFrom`, `vectorRatio`/`vectorRatioFrom`, and vector-slice ratio fills; power-of-two denominators use packed bit chunks. Local rows: 1/4 about 2.85B samples/s and 4.69B lanes/s, 3/8 about 2.1B samples/s and 3.87B lanes/s, 1/2 about 3.0B samples/s and 9.85B lanes/s | Adopted for exact high-volume probability sampling without converting ratios to floats. |
| Direct dynamic weighted-tree sampling | `WeightedTree.sampleFrom` was about 46-48M ops/s and `WeightedIntTree.sampleFrom` about 50-52M ops/s in update+sample microbenchmarks | Adopted for API consistency and direct-engine workflows; performance is roughly neutral for generic tree and comparable for integer tree. |
| Direct static weighted sampling | Rust `WeightedIndex` row covers repeated weighted-index sampling | `AliasTable.sampleFrom` plus `WeightedChoice.sampleFrom` / `sampleValueFrom` add direct-source O(1) static weighted sampling paths; local rows are about 126M samples/s for facade calls and about 122-125M for direct-source calls | Adopted for static weighted-choice ergonomics and performance evidence; current direct path is API-quality work, not a speed win. |
| Static weighted sampler fill APIs | Static repeated weighted sampling had direct single-sample paths but no allocation-free bulk fill methods on the reusable samplers | Added `AliasTable.fill` / `fillFrom`, `WeightedTree.fill` / checked fill variants, and `WeightedIntTree.fill` / checked fill variants. Bulk fill now validates and caches the total weight once per slice. Local rows: alias fill direct about 127M samples/s, weighted-tree fill direct about 73M, and weighted-int-tree fill direct about 90M. | Adopted for high-volume static weighted sampling; dynamic update+sample remains a separate benchmark shape. |
| Checked discrete distribution fill helpers | Public distribution-layer bulk fills preserved assert-fast paths but user-supplied parameters still required manual sampler construction for fallible bulk workflows | Added checked direct-source bulk helpers for Bernoulli, Binomial, NegativeBinomial, Hypergeometric, Poisson, Geometric, and GeometricFailures, with tests for valid fills and invalid parameters. | Adopted for ergonomics and safety; this is an API consistency improvement rather than a throughput change. |
| Checked core continuous distribution fill helpers | Core continuous/derived distribution-layer fills also preserved assert-fast paths but user-supplied parameters still required manual sampler construction for fallible bulk workflows | Added checked direct-source bulk helpers for LogNormal, HalfNormal, Gamma, ChiSquared, Chi, Erlang, Beta, FisherF, and StudentT, with tests for valid fills and invalid parameters. | Adopted for ergonomics and safety; this is an API consistency improvement rather than a throughput change. |
| Checked tail/bounded distribution fill helpers | Tail and bounded distribution-layer fills had the same fallible bulk workflow boilerplate gap | Added checked direct-source bulk helpers for Triangular, Arcsine, Cauchy, Laplace, Logistic, LogLogistic, Kumaraswamy, PowerFunction, Rayleigh, Maxwell, Pareto, Weibull, Gumbel, Frechet, SkewNormal, and PERT, with tests for valid fills and invalid parameters. | Adopted for ergonomics and safety; this is an API consistency improvement rather than a throughput change. |
| Checked inverse-Gaussian-family fill helpers | InverseGaussian and NormalInverseGaussian bulk fills had the same fallible workflow boilerplate gap after their staged/vectorized production fills were added | Added `fillInverseGaussianChecked` / `fillInverseGaussianCheckedFrom` and `fillNormalInverseGaussianChecked` / `fillNormalInverseGaussianCheckedFrom`, with tests for valid fills and invalid parameters. | Adopted for ergonomics and safety; this is an API consistency improvement rather than a throughput change. |
| Zipf/Zeta top-level fill helpers | Zipf and Zeta reusable samplers exposed `fill` / `fillFrom`, but there were no module-level bulk helpers matching the rest of the distribution surface | Added `fillZipf` / `fillZipfFrom` / checked variants and `fillZeta` / `fillZetaFrom` / checked variants, with tests for valid fills and invalid parameters. | Adopted for API consistency; this is a surface-completeness improvement rather than a throughput change. |
| Distribution-level normal/exponential fill wrappers | `Rng` exposed bulk normal/exponential helpers, but the distributions module only exposed reusable sampler fills and single-sample top-level helpers | Added `fillNormal` / `fillNormalFrom` / checked variants and `fillExponential` / `fillExponentialFrom` / checked variants as module-level wrappers over the existing optimized `Rng` bulk paths. | Adopted for namespace consistency; throughput remains governed by the existing benchmarked `Rng` kernels. |
| Distribution-level uniform fill wrappers | The distributions module exposed single-shot `uniform` / `uniformInclusive` and reusable `Uniform(T).fill`, while `Rng` exposed the corresponding optimized bulk range fills | Added `fillUniform` / `fillUniformFrom` / checked variants and `fillUniformInclusive` / `fillUniformInclusiveFrom` / checked variants for integer and floating-point ranges. | Adopted for namespace consistency; ordinary exclusive range throughput remains governed by the existing benchmarked `Rng.fillRangeFrom` path. |
| WeightedTree inline traversal probe | Generic `WeightedTree.fillFrom` remained slower than the unsigned integer tree, so tried inlining the sample traversal over `subtotals.items` in an isolated probe | `weighted-tree-probe` showed current fill about 73.8M samples/s and the inline traversal candidate about 71.3M with identical checksum. | Rejected. Keep the existing `sampleCheckedFrom` traversal; the generic float tree gap is not solved by local inlining. |
| Choice / WeightedChoice fill APIs | Sequence reusable samplers exposed single-sample and iterator workflows but no allocation-free bulk fill methods | Added `Choice.sampleFrom`, `sampleValueFrom`, `fill`, `fillFrom`, `fillValues`, `fillValuesFrom`, plus `WeightedChoice.fill` / `fillFrom` / value-fill variants. Local row: `weighted choice fill direct` about 129M samples/s, matching direct single-value sampling. | Adopted for high-volume repeated collection sampling and API consistency with distribution samplers. |
| Iterator sampling direct-source APIs | Iterator sampling helpers still required an `Rng` facade while index sampling already had direct-source `From` variants | Added `chooseIteratorFrom`, `sampleIteratorFrom`, `chooseIteratorWeightedFrom`, and `sampleIteratorWeightedFrom`, with unit coverage. Existing direct index/weighted benchmark rows continue to compile and run. | Adopted for comptime-known engine workflows over streaming iterators without collecting first. |
| Reusable/direct `SkewNormal(T).sampleFrom` | `rand_distr skew-normal`: about 263M samples/s for shape=1; shape=2 row about 204M | Adopted as scalar-fast reusable sampler path. Generic non-`0`/`+/-1` shapes now use the equivalent delta/Azzalini form. Latest `skew-normal-probe -- 4194304` still shows shape=2 scalar sample-loop throughput about 292M samples/s, above the local Rust shape=2 row; shape=1 ScalarPrng stays about 262M and remains near the local Rust boundary on the original max-normal special branch. |
| SkewNormal bulk fill APIs | Local Rust evidence uses repeated sampler calls | Added `fillSkewNormal`, `fillSkewNormalFrom`, and `SkewNormal.fill` / `fillFrom` for high-volume slices. `SkewNormal.fillFrom` now routes through the module-level fill helper, and generic non-`0`/`+/-1` shapes use the delta/Azzalini form. Latest rows are about 186M facade fill, 224M direct-source fill for shape=1, and 231M direct-source fill for shape=2. | Adopted for API consistency and allocation-free bulk workflows; generic shape=2 fill now exceeds the local Rust shape=2 row, while scalar single-sample skew-normal remains watch. |
| SkewNormal staged bulk probe | Tried staging two normal buffers and combining them with scalar or vector min/max/linear transforms to reduce per-sample call overhead | Latest `skew-normal-probe -- 4194304` remains mixed: FastPrng shape=1 staged vector is about 214M versus current 203M, but ScalarPrng shape=1 staged vector is about 262M versus current 262M, shape=2 staged vector is about 200M FastPrng / 234M ScalarPrng versus current 204M / 263M, and checksums differ for staged variants. | Rejected as a default production path. Keep current scalar draw order and revisit only with a staged algorithm that does not regress the scalar-fast profile. |
| SkewNormal Box-Muller pair source | SkewNormal consumes pairs of standard normals, so Box-Muller could generate a natural pair per sample instead of two ziggurat calls | `skew-normal-probe` rows were only about 45-49M samples/s for shape=1/2 across FastPrng and ScalarPrng, far below current ziggurat-backed rows around 188-264M. | Rejected. Transcendental-heavy Box-Muller is not viable for SkewNormal performance on the current Linux target. |
| Move `SkewNormal` shape +/-1 min/max into special branches | No measurable win: scalar direct stayed around 228M samples/s and occasionally dipped slightly | Rejected. The bottleneck is the two normal samples, not the min/max placement. |
| Cache `SkewNormal` generic-shape coefficients in reusable sampler | Tried storing method tags and precomputed high/low linear coefficients in `SkewNormal(T)` and routing `fillSkewNormalFrom` through the reusable sampler | Shape=2 did not improve: facade stayed about 172-173M and scalar direct fell from about 171M to about 166M; shape=1 scalar direct also regressed from about 180M to about 173M in the same benchmark shape. | Rejected. Keep the compact sampler and direct `skewNormalFrom` path; future work needs a faster normal pair source or different algorithm. |
| Unit geometry signed-unit float generation | Replacing `2 * float - 1` with exponent-bit construction for internal `[-1, 1)` candidates raised `unit circle`/`unit disc`/`unit sphere` scalar direct from about 113-120M to about 150-162M samples/s and `unit ball` direct from about 44M to about 59M | Adopted. This matches Rust's efficient uniform-float range strategy without changing the public API. |
| Unit geometry bulk fill APIs | Local Rust rows use repeated sampler calls | Added `fillUnitCircle`, `fillUnitDisc`, `fillUnitSphere`, `fillUnitBall`, sampler `fill` methods, and direct-source `From` variants for array-backed point slices. Initial local rows: fill circle about 126M, fill disc about 141M, fill sphere about 129M, fill ball about 55M samples/s. Latest `unit-geometry-probe -- 4194304` rows are about 152M current / 154M batched unit circle, 170M current / 171M batched unit disc, 157M current / 158M batched unit sphere, and 70M current unit ball samples/s. | Adopted for high-volume geometry ergonomics and modest bulk speedups across all unit geometry families. |
| UnitBall candidate batch multiplier | Tried increasing batched candidate generation from the production shape to 2x/3x/4x the remaining output count | Latest `unit-geometry-probe -- 4194304` still shows production current fill about 70M samples/s, while explicit x2/x3/x4 candidate batches are about 67.5M, 61.1M, and 53.2M respectively. | Rejected. Keep the current production UnitBall fill shape; larger candidate batches waste too many generated points and hurt cache behavior. |
| Public `inline` markers on unit-geometry helpers | Adding `inline` to the exported unit-geometry functions did not produce an independent benchmark win once signed-unit float generation was isolated | Rejected. Keep public function declarations normal and let the optimizer handle call boundaries. |
| Default `fillNormal(f32)` via vector Box-Muller | About 125M samples/s, slower than scalar ziggurat bulk around 196M samples/s | Rejected as default. Keep explicit vector normal prototype for experimentation. |
| Paired `fillVectorNormal(f32)` vector generation | `vectorbench` improved from about 74M to about 130M lanes/s | Superseded by scalar ziggurat lane-fill; the single-vector default now also uses scalar ziggurat lanes. |
| Scalar ziggurat lane-fill for vector normal/exponential | Earlier vector math kernels were slower than scalar ziggurat lanes | `vectorNormal` / `vectorExponential` and vector-slice fills now use scalar ziggurat lanes for `f32`/`f64`. Local rows: single-vector `vector normal f32x8` rose from about 75M to about 432M lanes/s, `vector exponential f32x8` from about 196M to about 393M; latest `vectorbench` rows are about 497M normal f32x8, 497M normal f64x4, 471M exponential f32x8, and 466M exponential f64x4 lanes/s after the exponential mantissa-threshold fast path. | Adopted as the default until a true SIMD distribution kernel beats scalar ziggurat lane-fill. |
| Vector standard normal/exponential API wrappers | Stage 4 vector ergonomics needed named standard-distribution helpers so high-volume vector callers do not pass magic `(mean=0, stddev=1)` or `rate=1` parameters | Added `vectorStandardNormal`, `vectorStandardNormalFrom`, `fillVectorStandardNormal`, `fillVectorStandardNormalFrom`, `vectorStandardExponential`, `vectorStandardExponentialFrom`, `fillVectorStandardExponential`, and `fillVectorStandardExponentialFrom`, plus checked single-vector `vectorNormalChecked` / `vectorExponentialChecked` for user-supplied parameters. Dedicated `vectorbench` rows now cover the standard wrappers directly; a local validation run showed the wrappers tracking the parameterized rows within noise at about 392M vs 393M normal f32x8, 363M vs 363M normal f64x4, 370M vs 369M exponential f32x8, and 361M vs 362M exponential f64x4 lanes/s. | Adopted for API ergonomics and checked-parameter consistency; not evidence of a new true-SIMD distribution kernel. |
| Vector ziggurat fast-path with scalar repair | Tried a true-SIMD prototype that builds four ziggurat candidates at once, accepts fast-path lanes with vector comparisons, and repairs rare rejected lanes through scalar ziggurat sampling | `ziggurat-probe` rows were about 452M lanes/s for normal f64x4 and 435M lanes/s for exponential f64x4, below the scalar raw probe rows and below current scalar lane-fill vectorbench rows around 497M normal and 449M exponential lanes/s. | Rejected as a default path. The current vector-gather/scalar-repair overhead outweighs the fast-path SIMD gain; keep scalar lane-fill until a denser SIMD kernel wins. |
| `fillVectorNormal(f64)` vector Box-Muller kernel | `vectorbench` reports about 93M lanes/s, slower than scalar ziggurat lane-fill | Rejected as a default path. |
| `fillVectorExponential(f64)` vector log kernel | `vectorbench` reports about 119M lanes/s, slower than scalar ziggurat lane-fill | Rejected as a default path. |
| Default `fillExponential(f32)` via vector log kernel | About 183M samples/s, slower than scalar ziggurat bulk around 320M samples/s | Rejected as default. Keep explicit vector exponential prototype for experimentation. |
| Isolated vector-slice range fill benchmark | Full benchmark rows for vector-slice range fill previously caused anomalously long runs | `zig build -Doptimize=ReleaseFast -Dcpu=native vectorbench` now covers vector-slice range fill without slowing the full benchmark. Latest rows: `fillVectorRange f32x8` about 1.01B lanes/s and `fillVectorRange f64x4` about 695M lanes/s. | Adopted. Keep vector-slice range evidence in `vectorbench` instead of the full throughput benchmark. |

## Next Candidate

Continue performance triage on the remaining large gaps:

- normal scalar ziggurat paths still trail local `rand_distr`,
- exponential scalar paths are closer after the mantissa-threshold fast path,
  but still sit below local `rand_distr` rows in some full-benchmark shapes,
- `fillOpenClosed(f64)` bulk is now at the local Rust evidence noise boundary,
  but remains a watch item because exact `(0, 1]` conversion is sensitive to
  expression shape,
- Cauchy bulk fill is now at the local Rust evidence boundary, while scalar
  single-sample Cauchy remains a small watch item,
- log-normal scalar direct still trails local `rand_distr`, while log-normal,
  inverse-Gaussian, and NIG bulk fills now exceed the local Rust rows,
- skew-normal shape=1 remains a local Rust boundary item; generic shape=2 is
  closed in isolated sample-loop and fill-loop evidence,
- unit geometry bulk fills now have modest speed wins across all four families,
  but scalar-direct profiles remain a useful reference,
- SIMD/vector distribution kernels need stronger default-path wins before they
  can replace scalar ziggurat paths.
