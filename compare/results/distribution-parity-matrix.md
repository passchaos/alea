# Distribution Parity-Plus Matrix

This document compares `alea`'s distribution surface with the locally available
Rust references:

- `~/Work/rand`, current default `rand` checkout
- cached historical `rand` crates under `~/.cargo/registry/src`

The goal is not to mirror Rust traits or feature gates. The goal is to ensure
that core distribution functionality is covered or intentionally excluded in a
Zig-native way.

Local `rand_distr::*Error` discovery names are covered by Alea distribution
error aliases over the shared `distributions.Error` set, including
`NormalError`, `ExpError`, `GammaError`, `BetaError`, `BinomialError`,
`CauchyError`, `ChiSquaredError`, `FisherFError`, `FrechetError`, `GeoError`,
`GumbelError`, `HyperGeoError`, `InverseGaussianError`,
`NormalInverseGaussianError`, `ParetoError`, `PertError`, `PoissonError`,
`SkewNormalError`, `TriangularError`, `WeibullError`, `ZetaError`, and
`ZipfError`.
Local `rand_distr` scalar sampler `new(...)` constructor names are covered by
Rust-discoverable `new` aliases over Alea's canonical `init` constructors where
the parameter semantics match. `Geometric` intentionally keeps Alea's one-based
trial-count semantics and exposes local `rand_distr` failure-count construction
through `GeometricFailures.new`.

## Current Rust `rand` Default Crate

| Rust `rand` area | Alea status |
| --- | --- |
| `StandardUniform` integers/floats/bools/bytes | Covered by `distributions.StandardUniform`, `Rng.value`, Rust-discoverable `randomValue` aliases, scalar helpers, `float`, `boolean`, caller-owned bytes, and allocation-returning `bytesAlloc` |
| `StepRng` test/mock source | Covered by `StepRng`, root `stepRng`, and root `constRng`; byte fills use the local Rust `StepRng(255, 1)` little-endian word shape and raw aliases integrate with `RngReader` / `std.Random` workflows |
| `StandardUniform` arrays/tuples | Covered by `distributions.StandardUniform` and `Rng.value` / `randomValue` arrays and tuples plus allocation-returning `Rng.valueBatch` / `Rng.valueBatchChecked` repeated samples |
| `StandardUniform` char | Covered by explicit `Rng.unicodeScalar`, caller-owned `fillUnicodeScalar`, owned `unicodeScalarBatch`, range helpers, and Unicode UTF-8 helpers, including caller-owned-buffer UTF-8 output |
| `Uniform` integer/float ranges | Covered by `Uniform(T)` with Rust-discoverable `new` / `newInclusive` aliases, `tryFromRange` / `tryFromRangeInclusive` aliases for local Rust `Uniform::try_from(Range)` / `RangeInclusive` workflows, `UniformInt(T)` / `UniformFloat(T)` / `UniformUsize` backend discovery aliases, Rust-discoverable `UniformError` including `NonFinite` diagnostics for non-finite float endpoints/widths, Rust-discoverable one-shot checked `sampleSingle` / `sampleSingleInclusive` aliases, `Rng.randomRange` / `randomRangeAtMost` aliases for local Rust `Rng::random_range` workflows, `VectorUniform(VectorType)` with matching reusable aliases, `Open01`, `OpenClosed01`, scalar/vector range helpers, caller-owned range/strict-interval fills, owned `Rng.rangeBatch` / `vectorRangeBatch` / `openBatch` / `openClosedBatch` / `vectorOpenBatch` / `vectorOpenClosedBatch` batches, endpoint/inclusivity, and range/strict-interval moment accessors |
| `UniformChar` | Covered by reusable `UniformUnicodeScalar` plus `UniformChar` as a Rust-discoverable alias, `unicodeScalarRangeLessThan` / `unicodeScalarRangeAtMost`, caller-owned `fillUnicodeScalarRangeLessThan` / `fillUnicodeScalarRangeAtMost`, and owned `unicodeScalarRangeLessThanBatch` / `unicodeScalarRangeAtMostBatch`; Zig uses `u21` scalar values instead of a native `char` type |
| `UniformDuration` | Covered by reusable `distributions.UniformDuration`, `durationRangeLessThan`, `durationRangeAtMost`, and owned duration range batches for `std.Io.Duration` |
| `Bernoulli` | Covered by `Bernoulli` with Rust-discoverable `new` / `newRatio` / `fromRatio` aliases, Rust-discoverable `p()` probability lookup, Rust-discoverable `BernoulliError` alias, and vector equivalents; `Rng.randomBool` / `randomRatio` aliases for local Rust `Rng::random_bool` / `random_ratio`; `chance`, `ratio`, checked variants, caller-owned and allocation-returning scalar/vector chance/ratio batches, and probability/moment/mode/support accessors |
| `Alphanumeric`, `Alphabetic`, and `SampleString` for reusable char choices | Covered by distribution-namespace `Alphanumeric` / `Alphabetic` aliases plus ASCII charsets and `Charset`, including charset count/constantIndex/byte/emptiness/membership/checked-item/optional-item/probability/probability-iterator size-hint diagnostics and SampleString-style `sampleString` / `appendString` aliases; `UnicodeCharset` covers reusable Unicode scalar alphabets with matching UTF-8 string sample/append workflows analogous to local Rust `SampleString for Choose<char>` while preserving Zig `u21` scalar representation |
| `Slice::Choose`, `IteratorRandom`, and fixed-size slice samples | Covered by distribution-namespace `Choose(T)`, `distributions.slice.Choose(T)` / `distributions.slice.Empty` discovery aliases, `Rng.chooseIndex`, `Rng.chooseIndexArray`, `Rng.chooseIndexU32`, `Rng.chooseIndexArrayU32`, `Rng.choose`, `Rng.chooseValueArray`, `Rng.chooseConstPtr`, `Rng.chooseConstPtrArray`, `Rng.choosePtr`, `Rng.choosePtrArray`, `seq.chooseIndex`, `seq.chooseIndexArray`, `seq.chooseIndexU32`, `seq.chooseIndexArrayU32`, `seq.choose`, `seq.chooseConstPtr`, `seq.choosePtr`, `seq.chooseRepeatedValueArray`, `seq.chooseRepeatedConstPtrArray`, `seq.chooseRepeatedPtrArray`, `seq.fillChooseIndex`, `seq.fillChooseIndexU32`, `seq.fillChoose`, `seq.fillChooseConstPtr`, `seq.fillChoosePtr`, `seq.chooseIndexBatch`, `seq.chooseIndexU32Batch`, `seq.chooseBatch`, `seq.chooseConstPtrBatch`, `seq.choosePtrBatch`, full in-place `seq.shuffle` / `seq.shuffleFrom`, head-selected `partialShuffle*` and Rust-style tail-selected `partialShuffleTail*`, `seq.Choice` Rust-discoverable `new` aliases, numChoices, constantIndex, checked item alias and optional item lookup, optional probability, and probability-iterator size-hint diagnostics, value/pointer/index samples, fills, fixed-size value/pointer/index arrays, owned value/pointer/index batches, and index/index-u32 streams, `chooseIter`, `chooseArray`, `sampleItemsArray`, `sampleArray`, and `sampleArrayU32`; item-slice/emptiness diagnostics plus single, fixed-size repeated value/const-pointer/mutable-pointer choice arrays and fixed-size no-replacement value/const-pointer/mutable-pointer arrays with both `choose*Array` and `sample*Array` naming, allocated value/const-pointer/mutable-pointer subset slices with both `chooseMultiple*` and `sampleItems*` / `samplePtrs*` / `sampleMutPtrs*` naming, owned sampled value/const-pointer/mutable-pointer iterators with exact-size len/sizeHint/fill helpers and bounded IndexVec iterator fill helpers, `seq.IndexedSamples` / `seq.SliceChooseIter` discovery aliases for sampled const-pointer iterators, caller-owned value/const-pointer/mutable-pointer subset buffers with matching sample aliases, allocated value/const-pointer/mutable-pointer reservoir samples, caller-owned value/const-pointer/mutable-pointer reservoir buffers, compact `IndexVec` owned-backing adoption, Rust-discoverable and checked index lookup, representation-preserving deep clone, Rust-discoverable consuming intoVec, consuming index iteration, lazy/caller-owned/allocation-returning/consuming value/const-pointer/mutable-pointer/u32 export mapping with exact-size len/sizeHint helpers, and cross-backing content equality, plus stable and hint-sensitive streaming iterator choices, fixed-size iterator, and caller-owned iterator reservoir exports are available |
| `WeightedIndex` | Covered by `distributions.WeightedIndex` as a Rust-discoverable alias for `AliasTable`, by `distributions.weighted.WeightedIndex` for the local Rust `rand::distr::weighted::*` module path, `distributions.WeightError` / `WeightedError` / `weighted.Error` aliases for the weighted error name plus static weighted `InvalidInput` / `InvalidWeight` / `InsufficientNonZero` / `Overflow` diagnostics, plus `Rng.weightedIndex`, generic `seq.weightedIndex`, `AliasTable`, and `WeightedChoice`; checked paths reject non-finite weights and overflowing totals, `AliasTable.updateWeights` / `WeightedChoice.updateWeights`, `AliasTable.updateMany` / `WeightedChoice.updateMany`, `AliasTable.updateAt` / `WeightedChoice.updateAt`, plus `AliasTable.update` / `WeightedChoice.update` support ordered partial, single-weight, and full weight replacement, and `WeightedChoice.indexIter` / `indexIterU32` expose repeated weighted index streams, and `WeightedChoice.iter` / `seq.chooseWeightedIter` / `seq.chooseWeightedIterBy` / `seq.chooseWeightedIterByIndex` expose repeated weighted const-pointer streams |

## Historical Rust `rand` Non-Uniform Distributions

| Rust distribution family | Alea status |
| --- | --- |
| Normal / StandardNormal | Covered: `standardNormal`, `StandardNormal(T)`, `normal`, `Normal(T)`, `Rng.standardNormalBatch`, `Rng.normalBatch`, `Rng.vectorStandardNormalBatch`, `Rng.vectorNormalBatch`, including coefficient-of-variation construction/accessor, z-score conversion, standard-parameter/moment/median/mode/support accessors, and `meanParameter` / `stddevParameter` / `stdDevParameter` aliases for local `rand_distr::Normal::{mean, std_dev}` discovery; explicit opt-in `StandardNormalNativeF32` / `NormalNativeF32` / `VectorNormalNativeF32` provide separate native-f32 throughput/output profiles, and `VectorStandardNormalTableF32` / `VectorNormalTableF32` and `VectorStandardNormalTableF64` / `VectorNormalTableF64` provide vector-only table-quantile throughput/output profiles |
| LogNormal | Covered: `logNormal`, `LogNormal(T)`, including linear-space mean/coefficient-of-variation construction and accessors, median/mode diagnostics, z-score conversion, log-space, moment, support accessors, f32/f64 benchmark rows, explicit opt-in native-f32 profile `LogNormalNativeF32` / `VectorLogNormalNativeF32`, explicit opt-in f32 approximation paths `LogNormalApproxF32` / `LogNormalExp2F32`, and combined native-normal/exp2 opt-in profile `LogNormalNativeExp2F32` / `VectorLogNormalNativeExp2F32` |
| Exponential / Exp1 | Covered: `standardExponential`, `StandardExponential(T)`, `Exp1(T)` alias, `exponential`, `Exponential(T)`, `Exp(T)` alias, `Rng.standardExponentialBatch`, `Rng.exponentialBatch`, `Rng.vectorStandardExponentialBatch`, `Rng.vectorExponentialBatch`, including reusable-sampler and standard rate/moment/median/mode/support accessors plus infinite-rate point masses; explicit opt-in `StandardExponentialNativeF32` / `ExponentialNativeF32` / `VectorExponentialNativeF32` provide separate native-f32 throughput/output profiles, `VectorStandardExponentialApproxLogF32` / `VectorExponentialApproxLogF32` provide a vector-only f32 approximate-log throughput profile, and `VectorStandardExponentialTableF32/F64` / `VectorExponentialTableF32/F64` provide vector-only table-quantile throughput/output profiles |
| Gamma | Covered: `gamma`, `Gamma(T)`, including shape/scale/moment/mode/support accessors, collapsed `scale == 0` point masses, and local `rand_distr`-compatible infinite shape/scale infinity states |
| ChiSquared | Covered: `chiSquared`, `ChiSquared(T)`, including dof/moment/mode/support accessors, collapsed `dof == 0` point masses, and local `rand_distr`-compatible infinite-dof infinity states |
| FisherF | Covered: `fisherF`, `FisherF(T)`, including d1/d2, finite-moment, support accessors, and local `rand_distr` infinite-dof NaN/stream-shape behavior |
| StudentT | Covered: `studentT`, `StudentT(T)`, including dof/finite-moment/support accessors and local `rand_distr` infinite-dof NaN/stream-shape behavior |
| Gumbel | Covered: `gumbel`, `Gumbel(T)`, including location/scale/moment/median/mode/support accessors and collapsed `scale == 0` point masses |
| Frechet | Covered: `frechet`, `Frechet(T)`, including location/scale/shape, finite-moment, median/mode, and support accessors plus collapsed `scale == 0` and infinite-shape point masses |
| SkewNormal | Covered: `skewNormal`, `SkewNormal(T)`, including location/scale/shape parameter aliases, moment/support accessors, and collapsed `scale == 0` point masses |
| Pert | Covered: `pert`, `Pert(T)`, including default-shape, mean, range-builder constructors, builder diagnostics, parameter/moment accessors, collapsed `min == mode == max` point masses, and local `rand_distr` finite-range infinite-shape NaN/stream-shape behavior |
| UnitCircle / UnitDisc | Covered: `unitCircle`, `unitDisc`, `UnitCircle(T)`, `UnitDisc(T)`, including geometry, coordinate-moment, and radial-moment accessors |
| UnitSphere / UnitBall | Covered: `unitSphere`, `unitBall`, `UnitSphere(T)`, `UnitBall(T)`, including geometry, coordinate-moment, and radial-moment accessors |
| Poisson | Covered: `poisson`, `Poisson`, large-lambda Ahrens-Dieter path, local `rand_distr` max-lambda rejection, Alea zero-lambda point-mass extension, and lambda/moment/support accessors |
| Binomial | Covered: `binomial`, `Binomial`, exact small/p=0.5/large rejection paths, explicit sparse Poisson approximation helper, and trials/probability/moment/support accessors |
| Geometric / StandardGeometric | Covered: one-based `Geometric`, rand-style failure-count `GeometricFailures`, probability/moment/mode/support accessors, and p=0.5 `StandardGeometric` fast path/accessor |
| Hypergeometric | Covered: `Hypergeometric`, including parameter/moment/support accessors and HIN inverse-transform fast path for small-mode regimes; large-parameter H2PE-equivalent performance remains tracked in `performance-triage.md` |
| WeightedChoice / weighted slice choice | Covered: one-shot `weightedIndex` / `Rng.weightedIndexU32` / `weightedIndexU32` plus `Rng.weightedIndexArray` / `Rng.weightedIndexU32Array` / `Rng.chooseWeightedValueArray` / `Rng.chooseWeightedConstPtrArray` / `Rng.chooseWeightedPtrArray` plus generic `seq.weightedIndexArray` / `seq.weightedIndexU32Array` / `seq.chooseWeightedValueArray` / `seq.chooseWeightedConstPtrArray` / `seq.chooseWeightedPtrArray` fixed repeated weighted arrays and `chooseWeighted` / `chooseWeightedConstPtr` / `chooseWeightedPtr`, accessor-based `weightedIndexBy` / `weightedIndexU32By` plus `weightedIndexArrayBy` / `weightedIndexU32ArrayBy` fixed repeated arrays and caller-owned repeated `fillWeightedIndexBy` / `fillWeightedIndexU32By`, allocation-returning repeated `weightedIndexBatchBy` / `weightedIndexU32BatchBy`, and `chooseWeightedBy` / `chooseWeightedConstPtrBy` / `chooseWeightedPtrBy` plus `chooseWeightedValueArrayBy` / `chooseWeightedConstPtrArrayBy` / `chooseWeightedPtrArrayBy` fixed repeated arrays, caller-owned repeated `fillChooseWeightedBy` / `fillChooseWeightedConstPtrBy` / `fillChooseWeightedPtrBy`, allocation-returning repeated `chooseWeightedBatchBy` / `chooseWeightedConstPtrBatchBy` / `chooseWeightedPtrBatchBy`, and `sampleWeightedIndexArrayBy` / `sampleWeightedIndexArrayU32By` / `sampleWeightedArrayBy` / `sampleWeightedPtrArrayBy` / `sampleWeightedMutPtrArrayBy`, `sampleWeightedIndicesBy` / `sampleWeightedIndicesU32By` / `sampleWeightedIndexVecBy`, index-weighted `weightedIndexByIndex` / `weightedIndexU32ByIndex`, caller-owned `fillWeightedIndexByIndex` / `fillWeightedIndexU32ByIndex`, allocation-returning `weightedIndexBatchByIndex` / `weightedIndexU32BatchByIndex`, value/const-pointer/mutable-pointer `chooseWeightedByIndex` / `chooseWeightedConstPtrByIndex` / `chooseWeightedPtrByIndex` plus caller-owned repeated `fillChooseWeightedByIndex` / `fillChooseWeightedConstPtrByIndex` / `fillChooseWeightedPtrByIndex` and allocation-returning repeated `chooseWeightedBatchByIndex` / `chooseWeightedConstPtrBatchByIndex` / `chooseWeightedPtrBatchByIndex`, and `sampleWeightedIndicesByIndex` / `sampleWeightedIndicesU32ByIndex` / `sampleWeightedIndexVecByIndex` plus caller-owned `sampleWeightedIndicesByIndexInto` / `sampleWeightedIndicesU32ByIndexInto` and fixed-size `sampleWeightedIndexArrayByIndex` / `sampleWeightedIndexArrayU32ByIndex` for Rust `index::sample_weighted(..., |index| ...)` workflows, `sampleWeightedBy` / `sampleWeightedPtrsBy` / `sampleWeightedMutPtrsBy`, plus caller-owned `sampleWeighted*ByInto` helpers for item-embedded or derived weights, allocation-returning `sampleWeighted` / `sampleWeightedPtrs` / `sampleWeightedIndicesU32`, compact `sampleWeightedIndexVec`, caller-owned `sampleWeightedIndicesInto` / `sampleWeightedIndicesU32Into` / `sampleWeightedInto` / `sampleWeightedPtrsInto`, fixed-size `sampleWeightedIndexArray` / `sampleWeightedIndexArrayU32` / `sampleWeightedArray` / `sampleWeightedPtrArray` / `sampleIteratorWeightedArray` / `sampleIteratorWeightedInto`, reusable Rust-discoverable `WeightedChoice.new`, item- or index-accessor-constructible `WeightedChoice` numChoices, positiveCount, and constantIndex diagnostics, value/pointer/index samples, fills, repeated pointer/index streams including item- and index-accessor pointer streams, fixed-size value/pointer/index arrays, updates, and owned value/pointer/index batches, item/emptiness diagnostics, checked item alias and optional item lookup, optional single-weight, lazy weight/probability-iterator size hints, probability diagnostics and exports, weight updates, single/bulk weight introspection, `chooseIteratorStable`, `sampleIteratorFill`, `chooseIteratorWeighted`, `sampleIteratorWeighted` |
| WeightedChoice / index-weight fixed arrays | Covered: fixed repeated with-replacement length/index-weight accessor arrays via `seq.weightedIndexArrayByIndex`, `seq.weightedIndexU32ArrayByIndex`, `seq.chooseWeightedValueArrayByIndex`, `seq.chooseWeightedConstPtrArrayByIndex`, and `seq.chooseWeightedPtrArrayByIndex`; these are intentionally distinct from no-replacement `seq.sampleWeightedIndexArrayByIndex` / `seq.sampleWeightedIndexArrayU32ByIndex` |
| WeightedAliasIndex | Covered: `AliasTable(Weight)` for O(1) repeated weighted sampling, including Rust-discoverable `new`, Rust-discoverable `updateWeights`, ordered partial `updateMany`, single-weight `updateAt`, `initBy` / `updateBy` item-accessor helpers, `initByIndex` / `updateByIndex` index-accessor helpers, `len`, `numChoices`, `isEmpty`, `totalWeight`, `positiveCount`, optional `weight`, lazy `weightIter` / `probabilityIter` size hints, `weightAt`, `probabilityAt`, deterministic `constantIndex`, `sampleIndex` / `fillIndices` aliases matching reusable `WeightedChoice` naming, compact `sampleU32` / `fillU32` output when populations fit `u32`, owned `indices` / `indicesU32` repeated-sample batches, fixed-size `indexArray` / `indexArrayU32` output, repeated `iter` / `iterU32` index streams, and allocation-returning or caller-buffer weight/probability reconstruction |
| WeightedTreeIndex | Covered: `WeightedTree(Weight)` for generic weights and `WeightedIntTree(Weight)` for faster unsigned integer sample/update/push/pop workloads, including Rust-discoverable `new`, Rust-discoverable `updateWeights`, ordered partial `updateMany`, `numChoices`, `positiveCount`, `constantIndex`, `isValid` for local `is_valid` sampling readiness diagnostics, `initBy` / `updateAllBy` item-accessor helpers, `initByIndex` / `updateAllByIndex` index-weight accessor helpers, `sampleIndex` / `fillIndices` aliases matching reusable `WeightedChoice` naming, compact `sampleU32` / `fillU32` output when populations fit `u32`, owned `indices` / `indicesU32` repeated-sample batches, fixed-size `indexArray` / `indexArrayU32` output, and `iter` / `iterU32` repeated index streams; both expose checked and optional single-weight/probability lookup plus lazy weight iterators and `weights` / `weightsInto` and `probabilities` / `probabilitiesInto` export for diagnostics; `WeightedIntTree` stores subtotals in `u64` and rejects wider integer values that do not fit |
| InverseGaussian | Covered: `inverseGaussian`, `InverseGaussian(T)`, including mean/shape/moment/support accessors and collapsed `mean == 0` / infinite-shape point masses |
| NormalInverseGaussian | Covered: `normalInverseGaussian`, `NormalInverseGaussian(T)`, including alpha/beta/gamma, moment/support accessors, and infinite-alpha point masses |
| Zipf | Covered: `zipf`, `Zipf(T)`, including n/exponent and support-bound accessors |
| Zeta | Covered: `zeta`, `Zeta(T)`, including exponent and support-bound accessors |

## Additional Alea Core Distributions

These are not in current Rust `rand` default crate, but are useful core random
toolkit functionality and reduce reliance on companion crates:

| Alea distribution | Status |
| --- | --- |
| Beta | Covered: `beta`, `Beta(T)`, including alpha/beta/moment/mode/support accessors and local `rand_distr` infinite-shape endpoint/NaN stream-shape behavior |
| Triangular | Covered: `triangular`, `Triangular(T)`, including min/mode/max, moment/median accessors, collapsed range point masses, and local `rand_distr` non-finite ordered-bound NaN/stream-shape behavior |
| Arcsine | Covered: `arcsine`, `Arcsine(T)`, including min/max/moment/median accessors and collapsed `min == max` point masses |
| Cauchy | Covered: `cauchy`, `Cauchy(T)`, including median/mode/scale, undefined-moment, support accessors, collapsed `scale == 0` point masses, and local `rand_distr` non-finite median / infinite-scale acceptance |
| Laplace | Covered: `laplace`, `Laplace(T)`, including location/scale/median/mode/moment/support accessors and collapsed `scale == 0` point masses |
| Logistic | Covered: `logistic`, `Logistic(T)`, including location/scale/median/mode/moment/support accessors and collapsed `scale == 0` point masses |
| Rayleigh | Covered: `rayleigh`, `Rayleigh(T)`, including scale/moment/median/mode/support accessors and collapsed `scale == 0` point masses |
| HalfNormal | Covered: `halfNormal`, `HalfNormal(T)`, including scale/moment/support accessors and collapsed `scale == 0` point masses |
| Maxwell | Covered: `maxwell`, `Maxwell(T)`, including scale/moment/mode/support accessors and collapsed `scale == 0` point masses |
| Chi | Covered: `chi`, `Chi(T)`, including dof/moment/mode/support accessors, collapsed `dof == 0` point masses, and infinite-dof infinity states through ChiSquared |
| Erlang | Covered: `erlang`, `Erlang(T)`, including shape/scale/moment/mode/support accessors and collapsed `scale == 0` point masses |
| LogLogistic | Covered: `logLogistic`, `LogLogistic(T)`, including scale/shape, finite-moment, median/mode, and support accessors plus collapsed `scale == 0` and infinite-shape point masses |
| Kumaraswamy | Covered: `kumaraswamy`, `Kumaraswamy(T)`, including alpha/beta/moment/median/mode/support accessors and infinite-shape boundary point masses |
| PowerFunction | Covered: `powerFunction`, `PowerFunction(T)`, including min/max/shape/moment/median accessors, collapsed `min == max` point masses, and infinite-shape upper-endpoint point masses |
| Pareto | Covered: `pareto`, `Pareto(T)`, including scale/shape, finite-moment, median/mode, and support accessors plus collapsed `scale == 0`, infinite-shape point masses, and local `rand_distr` infinite-scale acceptance |
| Weibull | Covered: `weibull`, `Weibull(T)`, including scale/shape/moment/median/mode/support accessors plus collapsed `scale == 0`, infinite-shape point masses, and local `rand_distr` infinite-scale acceptance |
| Dirichlet | Covered: `Dirichlet(T)` and `multi.Dirichlet(T)` for local `rand_distr::multi::Dirichlet` discovery, allocation and `sampleInto` APIs, including alpha/mean/variance/covariance export, dimension/total-alpha accessors, one-dimensional point masses, and single-infinite-alpha vertex point masses |
| Multinomial | Covered: `Multinomial`, including trials/category/probability lookup/export and count moment lookup/export accessors |
| NegativeBinomial | Covered: `NegativeBinomial`, including successes/probability/moment/support accessors |

## Explicit Out Of Scope

| Rust ecosystem feature | Rationale |
| --- | --- |
| `Distribution<T>` trait mirroring | Zig-native sampler structs plus `Rng.sample`, `Rng.sampleIter`, and `Rng.sampleBatch` provide one-shot, iterator, and owned-batch reusable-sampler workflows without copying Rust traits |
| serde integration | Rust ecosystem-specific; not core RNG functionality for Zig |
| crate feature matrices | Rust packaging-specific |
| Rust SIMD `std::simd` distribution implementations | Zig vector/SIMD support should be designed as a separate Zig-native milestone if needed |

## Remaining Follow-Up

- Keep `linux-no-known-gaps-audit.md` current when local `rand` / `rand_distr`
  evidence or Alea's public distribution surface changes.
- Continue expanding benchmark parity where new local Rust rows reveal concrete
  performance gaps; current follow-up is tracked in `performance-triage.md`.
- Continue Zig-native vector/SIMD sampling through the constraints in
  `simd-distribution-kernel-notes.md`.
